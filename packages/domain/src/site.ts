/**
 * 서비스 웹사이트 주소.
 *
 * 앱(약관 링크) · 웹(공유 메타) · 도메인(정책 문서 주소)이 전부 여기서 읽는다.
 * weddingpick.kr은 보유한 커스텀 도메인이지만 현재 사용하지 않는다(2026-09-10 사용자 확인).
 * 재사용 결정과 연결 검증 전까지 실제 운영 중인 아래 주소를 유지한다.
 */
export const SITE_ORIGIN = 'https://weddingpick-web.onrender.com';
