/**
 * 서비스 웹사이트 주소.
 *
 * 앱(약관 링크) · 웹(공유 메타) · 도메인(정책 문서 주소)이 전부 여기서 읽는다.
 * 자체 도메인(weddingpick.kr)은 DNS가 아직 살아 있지 않다 — 그 주소를 걸면
 * «사이트를 찾을 수 없음»이 뜬다(2026-09-08 보고). DNS를 붙이면 이 값만 바꾼다.
 */
export const SITE_ORIGIN = 'https://weddingpick-web.onrender.com';
