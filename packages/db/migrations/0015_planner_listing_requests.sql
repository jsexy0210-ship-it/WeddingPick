-- 플래너 공개 근거를 만드는 절차.
--
-- 0008이 규칙과 제약을 세웠지만 **넣는 쪽이 없었다.** 공개하려면 근거가 있어야
-- 하는데 근거를 접수할 창구가 없어, 목록이 언제나 비어 있었다.
--
-- 개인 플래너는 인허가 데이터에 실리지 않는다(사업자가 아니다). 그래서 실질적으로
-- 가능한 근거는 vendor_official 하나다 — 소속 업체나 본인이 스스로 밝히는 것.
-- 그 요청을 받는 자리를 만든다.
--
-- 문의 창구(0009)를 그대로 쓴다. 접수·회신 경로·사람 결정·이력이 이미 있고,
-- 무엇보다 **노출 중단과 같은 창구여야** 한다. 올리는 문과 내리는 문이 따로 있으면
-- 한쪽만 관리되기 쉽다.

-- 등록 요청. 노출 중단(planner_delisting)의 반대편이다.
ALTER TYPE inquiry_category ADD VALUE 'planner_listing';
