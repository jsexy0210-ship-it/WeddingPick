# 웨딩픽 — 구현 규칙

이 저장소에서 화면을 만들거나 고칠 때 **매번** 지키는 규칙입니다. 판단이 갈리면 여기 적힌 값이 기준입니다.

---

## 0. 시작하기 전에 읽을 것

| 파일 | 언제 읽는가 |
|---|---|
| `spec/tokens.json` | 색 · 크기 · 간격 · 모션을 쓸 때 **항상** |
| `spec/strings.ko.json` | 문구를 넣을 때 **항상** |
| `spec/glossary.json` | 새 문구를 쓸 때 **항상** |
| `spec/screens.json` | 화면을 만들 때 |
| `README.md` | 처음 한 번 · 맥락 파악용 |
| `docs/design-system-policy.md` | 아이콘 · 픽셀 검수 · 공통 컴포넌트 작업 시 **항상** |

**하드코딩 금지.** hex · 폰트 크기 · 간격 · 문구를 코드에 직접 쓰지 않습니다. 전부 위 파일에서 가져옵니다.

---

## 1. 이 앱이 무엇인가

웨딩픽은 **웨딩업체를 찾아다니는 앱이 아닙니다.** 웨딩픽이 먼저 골라주고 사용자는 비교해서 Pick하는 앱입니다.

모든 핵심 화면은 이 순서를 따릅니다.

```
상황 → 추천 → 근거 → Pick
```

사용자가 정보를 직접 분석하게 만들지 않습니다. 판단 근거가 없으면 추천하지 않습니다.

**최상위 검수 질문** — 이 화면만 봐도 웨딩픽이 먼저 골라주고 사용자는 비교해서 Pick하는 서비스라는 것이 이해되는가. NO면 아무리 예뻐도 다시 만듭니다.

---

## 2. 심볼 — 절대 변경 금지

Pick Mark는 **하트 안에 체크**입니다. 아래 두 path가 확정본이며 어떤 이유로도 바꾸지 않습니다.

```
M12 20.5S3.5 15.2 3.5 9.9A4.4 4.4 0 0 1 12 8.1a4.4 4.4 0 0 1 8.5 1.8c0 5.3-8.5 10.6-8.5 10.6Z
M8.7 11.9l2.2 2.2 4.4-4.4
```

viewBox 24 · stroke 1.9 · round cap/join · fill none. 체크는 하트 중앙(12.0, 11.9).

적용처: 앱 아이콘 5종 · 스플래시 · 시작 화면 · Pick 탭 · Pick 완료 모션 · 웹 GNB · 관리자.

앱 아이콘과 스플래시는 **스킨과 무관하게 Coral `#FF6F61` 배경 + White 마크** 고정입니다.

---

## 3. 용어 — 사용자 화면 강제

`spec/glossary.json`이 원본이고, `lint-copy.js`가 CI에서 검사합니다.

### 절대 쓰지 않는 말

| 금지 | 대체 |
|---|---|
| AI | 웨딩픽 |
| 데이터 | 정보 |
| 결제인증 · 결제 확인 · 결제내역 제보 | Pick 인증 |
| 결제금액 / 결제일 / 결제상태 | 낸 금액 / 낸 날짜 / 상태 |
| 같은 거래 · 거래 내역 | 같이 묶을까요? · 등록한 내역 |
| 거의 · 아마 · 대략 · 어느 정도 · 가능성이 높음 · ~일 것으로 보임 | 확정 · 미확정 · 검증 전 · 확인 필요 · 조건부 · 측정값 · 추정값 |
| 탐색 | 검색 |
| 관심업체 · 찜 · 좋아요 | Pick |
| 중앙값 · 평균가 | 기준금액 |
| 평점 · 별점 | 이용한 사람들의 경험 |
| 리뷰 | 후기 |
| 파트너 · 동반자 | 배우자 |

내부 개발 · 관리자 · 비용관리 · API 필드명에서는 AI와 데이터를 그대로 씁니다. **사용자에게 보이는 순간만** 막습니다.

### 금액 표기 (고정)

```
152~184만원
확인된 정보 12건 · 최근 12개월 · 기준금액 168만원
```

검색 · TOP3 · 업체상세 · 비교 · 홈 추천에서 **동일하게** 씁니다.

### 확인된 정보 4단계

| 건수 | 표기 |
|---|---|
| 0~2 | `수집 중` — 금액 구간 비공개 |
| 3~4 | 구간 + 정보가 적다는 안내 |
| 5~9 | 구간만 |
| 10+ | 구간 + 기준금액 + 조건별 분포 |

빈 칸이나 `—`를 쓰지 않습니다.

### 추천 이유

`spec/strings.ko.json`의 `reason.*` 문장만 씁니다. 조건을 설명하는 문장을 새로 만들지 않습니다.

- ⭕ `고른 사진이랑 가장 비슷해요`
- ❌ `선택한 사진과 가장 유사한 스타일이에요`

### 문장 규칙

`-요`체. 느낌표 없음. 이모지 없음. 숫자에는 항상 단위.

```
섹션 제목    1줄 · 서브카피 사용하지 않음
안내         최대 2줄
CTA          1줄
Hero 제목    최대 2줄
Hero 서브    1줄 — 넘치면 폰트를 줄이지 말고 문장을 줄인다
```

같은 의미를 서로 다른 섹션에서 반복하지 않습니다. **화면당 Primary CTA는 1개**입니다.

---

## 4. 레이아웃

```
좌우 Gutter      24    전 화면 고정
섹션 하단        28
섹션 구분 밴드    16    #F2F3F6
리스트 행 최소    56
CTA Primary      52
CTA Secondary    48
입력 필드        52
탭바             72
터치 영역 최소    44
2열 카드 gap     11
```

Gutter 24의 예외는 full-bleed 이미지와 가로 캐러셀뿐이고, 그 경우에도 첫 콘텐츠 시작선은 24입니다.

**같은 행 카드의 CTA 하단선은 정확히 일치해야 합니다.** 이미지 비율 고정 + 텍스트 영역 고정 높이 + CTA `margin-top:auto`로 맞춥니다. 제목 · 금액 · 건수 · 설명은 각각 1줄이고 넘치면 말줄임합니다.

---

## 5. 색

Primary는 `#FF6F61` 코랄입니다. **CTA · Pick · 선택 상태 · 진행바에만** 씁니다. 색을 더 쓰는 방식으로 문제를 풀지 않습니다.

```
Ink        #212124    본문
Secondary  #4D5159    보조
Tertiary   #868B94    메타 · 건수
Disabled   #ADB1BA
Divider    #EAEBEE
Border     #DCDEE3
Paper      #FFFFFF
Recessed   #F7F8FA    카드
Band       #F2F3F6    섹션 구분
```

성공 `#1AA174` / 경고 `#805217` / 오류 `#E81607` / accent `#0088CC`.

스킨 6종은 사용자가 고르며 CTA · Pick · 선택 상태에만 적용됩니다. 의미색은 스킨과 무관하게 고정입니다. Dark Gray 스킨에서도 **Pick 버튼만 코랄을 유지**합니다.

---

## 6. 타이포

시스템 서체를 씁니다. **웹폰트를 넣지 않습니다.**

```
display  32/43  700    Hero
title    26/35  700    화면 제목
heading  24/32  700    시트 제목
section  20/27  700    섹션 제목 · 업체명
body     18/24  700|400
sub      16/22  700|400  본문 · 금액 · 버튼
caption  14/19  700|400  메타 · 건수
micro    13/18  700    배지
```

이 8단계 밖의 크기를 만들지 않습니다. 필요하면 `tokens.json`에 먼저 추가하고 전 화면에 반영합니다.

숫자에는 항상 tabular-nums.

---

## 7. 그림자와 모션

그림자를 거의 쓰지 않습니다. 구분은 `inset` 선과 배경 톤으로 합니다.

```
탭바 상단    inset 0 1px 0 #EAEBEE
행 구분선    inset 0 -1px 0 #EAEBEE
떠 있는 카드  0 2px 6px rgba(0,0,0,.16)
```

모션은 짧고 기능적입니다.

```
누름         scale(.97)      100ms
시트 진입     translateY      350ms  cubic-bezier(.16,1,.3,1)
체크 팝      scale 0→1.18→1  460ms  cubic-bezier(.34,1.56,.64,1)
요소 상승     translateY 10   420ms  cubic-bezier(.16,1,.3,1)
```

패럴랙스 · 스크롤 페이드인 · 반복 애니메이션(스피너 제외) · 장식용 모션은 쓰지 않습니다.

---

## 8. 상태 — 정상 하나만 그리면 미완성

각 화면에서 해당되는 상태를 **모두** 구현합니다.

```
회원       비회원 · 회원 · Pick 인증 회원
커플       미연결 · 초대 대기 · 연결됨
Pick       전 · 후 · 결정 완료
이미지     있음 · 없음(카테고리 기본으로 대체)
정보       0~2 · 3~4 · 5~9 · 10+
혜택       있음 · 없음 · 만료
로딩       Skeleton · 처리 중
오류       일반 · 네트워크 · 권한 거부 · 점검
```

**비회원에게 개인화 영역을 보여주지 않습니다.** 이름 · D-day · 진행률 · 취향 추천을 아는 척하지 않습니다.

**이미지가 없으면 «사진 준비 중» 상자를 노출하지 않습니다.** 카테고리 기본 이미지로 대체합니다.

**비교할 정보가 없으면 비교를 권하지 않습니다.** 눌러도 소득이 없는 버튼을 두지 않습니다.

---

## 9. 광고 독립성 — 구현 필수

광고비와 요금제 등급은 아래에 **영향을 주지 않습니다.**

```
웨딩픽 TOP3 · 오늘의 Pick · 개인화 추천
자연 검색순위 · 확인된 정보 · 후기 · 후기 순위
```

**광고 랭킹은 자연 추천과 분리된 별도 테이블로 관리합니다.** 추천 쿼리에 광고 필드를 조인하지 않습니다. 광고 노출 시 `광고` 라벨을 반드시 표시합니다.

요금제 — LIGHT 30,000원 / STANDARD 70,000원 / PREMIUM 150,000원. 첫 달 무료 또는 50% 할인 1회, 영구 무료 없음.

---

## 10. 탈퇴 시 데이터 처리

**탈퇴 = 개인정보 삭제.** 검증된 비식별 정보까지 삭제하는 것은 아닙니다.

| 즉시 삭제 | 분리 후 유지 | 별도 보관 |
|---|---|---|
| 계정 · 프로필 | Pick 인증 자료 → 목적 달성 시 삭제 | 신고 · 분쟁 기록 |
| 배우자 연결 | 비식별 정보 → 작성자 정보 제거 후 유지 | 법령상 보존 의무 정보 |
| Pick · 취향 · 개인화 | 작성 후기 → 작성자 정보 제거 후 유지 | |
| 일정 · 체크리스트 · 지출 | 업체별 금액 구간 · 건수 → 유지 | |

**`30일` 같은 임의의 통합 보존기간을 두지 않습니다.** 항목별 법정기간은 개인정보처리방침 확정 단계에서 정합니다.

---

## 11. 관리자 로그인

IP만으로 로그인을 유지하지 않습니다. **신뢰기기 + 장기 보안 세션 토큰**입니다.

IP는 보조 신호일 뿐이고, IP가 바뀌어도 신뢰기기면 로그아웃하지 않습니다. 비밀번호를 평문으로 로컬에 저장하지 않습니다.

재인증 조건 — 직접 로그아웃 / 비밀번호 · 권한 변경 / 신뢰기기 해제 / 비정상 접근 탐지.

---

## 12. 새 값이 필요할 때

디자인 시스템에 없는 값이 필요하면 이 순서로 처리합니다.

```
1. spec/tokens.json 에 토큰을 먼저 정의
2. 컴포넌트에 반영
3. 관련 화면 전체에 일괄 적용
```

화면 하나에서만 쓰는 임시 값을 만들지 않습니다.

---

## 13. PR 전 체크리스트

- [ ] `node lint-copy.js src/` 통과
- [ ] hex · 폰트 크기 · 간격을 하드코딩하지 않았는가
- [ ] 문구를 `strings.ko.json`에서 가져왔는가
- [ ] 좌우 Gutter 24를 지켰는가
- [ ] 같은 행 카드의 CTA 하단선이 일치하는가
- [ ] 화면당 Primary CTA가 1개인가
- [ ] Hero에서 같은 내용을 두 번 말하지 않는가
- [ ] 비회원 · 정보 부족 · 이미지 없음 · Pick 전후 상태를 구현했는가
- [ ] 광고가 자연 추천과 분리되어 있는가
- [ ] Pick Mark를 변형하지 않았는가
