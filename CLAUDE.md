# 웨딩픽 구현 정책 — 목업 일치 필수

이 문서는 모든 Claude 작업에 적용되는 강제 규칙이다.

## 절대 규칙

1. 디자인 목업과 실제 화면은 픽셀 단위로 일치해야 한다. 기능만 연결하거나 비슷하게 구현한 상태를 완료로 보고하지 않는다.
2. 구현 전 `docs/design-handoff/seed/`와 해당 목업을 읽는다. 목업·컴포넌트 시트·PROJECT_RULES의 값을 임의로 바꾸지 않는다.
3. 모든 화면은 목업 기준의 고정 샘플 데이터로 재현 가능해야 한다. API 미연결·비로그인·빈 응답을 이유로 목업과 다른 빈 화면을 검수 대상으로 사용하지 않는다.
4. 화면마다 실제 캡처를 생성해 목업과 비교한다. 차이가 있으면 수정하고 다시 캡처한다. 이 과정을 주요 화면 전부에 반복한다.
5. 같은 화면이 반복되면 라우팅·초기 진입 상태·탭 설정을 먼저 수정한다. “정상”으로 보고하지 않는다.
6. 사용자의 중간 승인을 기다리지 말고 안전한 범위에서 끝까지 진행한다. 막히면 질문 전에 로그·라우트·샘플 데이터를 점검한다.
7. 타입검사와 테스트가 통과해도 시각적 비교가 끝나지 않았으면 작업은 미완료다.

## 완료 조건

- 주요 화면별 목업/실행 캡처 비교 완료
- 색상·간격·폰트·문구·아이콘·CTA 위치 차이 수정 완료
- 비로그인/데이터 없음 상태에서도 목업 검수 가능
- Expo Router의 각 탭이 서로 다른 올바른 화면을 표시
- `npm run typecheck --workspace @weddingpick/mobile` 통과
- 변경 파일과 검수 결과를 보고하고 커밋

완료 보고에는 화면별 검수 목록, 남은 차이(없으면 “없음”), 검증 명령 결과, 커밋 해시를 반드시 포함한다.
