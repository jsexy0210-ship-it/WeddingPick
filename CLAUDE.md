# 웨딩픽 — 구현 규칙

## ⛔ 디자인 정본 — 절대 지침 (2026-09-24 대표 지시, 이 파일의 최우선)

대표님 원문(2026-09-24) — 「**절대적 지침. 디자인 원본은 위 경로로 고정한다**」 · 「**디자인 정본**」 ·
「**RN=…/docs/design/React_Native · 관리자, 랜딩=…/docs/design/html**」.

| 대상 | 정본(이 경로 하나뿐) |
| --- | --- |
| **앱(RN)** — 홈 · 검색 · Pick · 웨딩노트 · MY · 로그인·약관 동의 · 온보딩 · 로더 등 | `docs/design/React_Native/` |
| **관리자 · 랜딩** | `docs/design/html/` |

```
docs/design/React_Native/   React 변환본 v3.29.1(아이콘 보완판) 84개 파일 — 하위 폴더 없이 한 층으로 올라와 있다
  README.md · HANDOFF.md · CONVERSION_REPORT.md   범위 · 읽는 순서 · 결손 — 먼저 읽는다
  WeddingPick_App_Preview.html · preview.html     앱 화면 · 상태 프레임 80개 미리보기
  home · search · pick · note · my · common · components · devices  각각 .jsx(보드) + .js(모델: 화면별 값 · 스타일)
  *.svg · iconAssets.js · assetOverrides.js       아이콘 · 이미지 매핑
  runtime-check.json · source-map.json            화면 ID ↔ 원본 행 번호
docs/design/html/웨딩픽 관리자.dc.html · 웨딩픽 관리자 운영.dc.html · 웨딩픽 약관 방침.dc.html
docs/design/html/웨딩픽 랜딩 v4.dc.html · 웨딩픽 랜딩 하위페이지.dc.html
```

- **앱 화면은 `React_Native`만 근거로 쓴다.** `html/`의 앱 대메뉴 시안 6개(홈 · 검색 · Pick · 웨딩노트 ·
  MY · 공통 다이얼로그)는 **2026-09-24 대표님이 직접 지웠다** — git 이력에서 꺼내 근거로 쓰지 않는다
  (`canonical-manifest.json` `supersededOn20260924`). `README.md` · `PROJECT_RULES.md` · `CHANGELOG.md`는
  앱에 대해서는 `React_Native`와 어긋나면 진다. 어긋남을 발견하면 `React_Native` 기준으로 보고 무엇이 달랐는지 남긴다.
- **관리자 · 랜딩은 `html/`만 근거로 쓴다.** 해당 `.dc.html`의 화면 ID 구역을 연다.
- 두 곳 어디에도 없는 화면은 추측으로 만들지 않고 `DESIGN_SOURCE_NOT_VERIFIED`로 보고해 대표님께 받는다.
- 읽는 순서는 폴더 안 `HANDOFF.md`를 따른다 — 다만 거기 적힌 `src/boards/…` · `src/models/…` ·
  `reports/…` 경로는 **이 저장소에서는 한 층으로 풀려 있다**(`src/boards/home.jsx` → `home.jsx`,
  `src/models/home.js` → `home.js`, `reports/runtime-check.json` → `runtime-check.json`).
- 값은 `*.js` 모델과 `*.jsx` 보드에서 읽고, 필요하면 `preview.html`을 띄워 실제로 본다.
  `scripts/canon/extract-style.mjs`는 `.dc.html` 전용이라 앱 화면에는 그대로 맞지 않는다 — 손으로 추측하지 않는다.
- `HANDOFF.md` 자신의 경고를 그대로 지킨다: 이 폴더는 **React DOM 디자인 레퍼런스**이지 RN 완료본이 아니다.
  사이드바 · 상단 도구 · 휴대전화 바깥 번호 · 캔버스 설명은 탐색기이지 제품 UI가 아니다.
- 이 폴더의 파일을 바꾸거나 새로 올리면 `canonical-manifest.json`의 해시를 함께 갱신한다
  (`npm run test:design-canonical`이 센다).

## 현재 디자인 기준 — v3.29 (2026-09-23, 대표 지시 — 위 ⛔ 절대 지침이 이긴다)

대표님 원문(2026-09-23) — 「**최신파일이다 깃 저장소 디자인 유일 경로에 올려주고 작업진행해**」 ·
「**최신파일로 다 덮어씌워**」. v3.28(2026-09-22)을 잇는 같은 패턴의 지시다 — 새 전달본이
오면 그것이 정본이고 옛 것은 파기한다.

```
docs/design/README.md                전달본이 스스로 적은 규칙 — 먼저 읽는다
docs/design/PROJECT_RULES.md         v3.29에 신규 — 심볼·용어·레이아웃 고정 규칙(먼저 읽는다)
docs/design/CHANGELOG.md             v3.29가 무엇을 바꿨는지
docs/design/handoff-QA-체크리스트.md 개발 완료 뒤 전수 검수 항목
docs/design/html/                    시안 16개. 이것이 화면의 정본이다
```

**이 폴더 밖을 화면·값·문구의 근거로 쓰지 않는다.** v3.28의 9개(README·CHANGELOG·QA체크리스트·
html 6개)에서 **20개로 늘었다** — `PROJECT_RULES.md` 신규, html이 6개(대메뉴 5 + 약관방침)에서
16개로 늘었다: 대메뉴 5(홈·검색·Pick·웨딩노트·MY) · 공통 다이얼로그·빈상태·로더 · 웨딩픽
관리자 · 웨딩픽 관리자 운영 · 웨딩픽 약관 방침 · 웨딩픽 랜딩 v4 · 웨딩픽 랜딩 하위페이지 ·
웨딩픽 디바이스 대응 · 웨딩픽 컴포넌트 시트 · 웨딩픽 전체 IA · 웨딩픽 사용자 흐름 · 웨딩픽
스토어 이미지. 한 파일 안에서 화면은 위에서부터 1 · 2 · 3… 순서고 `N-1`은 그 위 화면의 하위
화면이며, 카드 위의 `WP-XXX-000`이 화면 ID다. 캔버스는 **375 × 812**다.

**2026-09-23에 v3.28을 파기했다.** 옛 6개 `.dc.html`과 v3.28판 README·CHANGELOG는
`canonical-manifest.json`의 `supersedes`에 적어 뒀다. 되살려 근거로 쓰지 않는다 —
지운 것은 git 이력에 남는다. **`docs/design/screen-inventory.md`는 아직 없다** — v3.28 6개
파일 전제로 짠 생성 스크립트(`scripts/build-screen-inventory.mjs`)가 v3.29의 16개(화면
카탈로그가 아닌 전체 IA·컴포넌트 시트·사용자 흐름·디바이스 대응·스토어 이미지 포함)에는
안 맞을 수 있어 재작업 전까지 비워 뒀다. 화면 대조표 없이 작업할 땐 해당 `.dc.html`을
직접 읽는다.

**핵심 메시지가 바뀌었다 — 「웨딩픽이 골라준다」계열 문구를 전부 지운다.** v3.29
PROJECT_RULES.md·README.md 원문 — 「**플래너 없이, 직접 고르는 웨딩 준비**. 웨딩픽은
업체를 대신 골라주지 않는다. 사용자가 직접 고르고, 웨딩픽은 실 제보 금액과 조건을
보여주는 역할이다.」 `웨딩픽이 골랐어요` · `웨딩픽이 골라드려요` · `웨딩픽 TOP3` · 추천 ·
결정사 · 플래너 대행 개념을 화면·카피·기능 어디에도 넣지 않는다. 코드에 이 표현이나
개념이 남아 있으면 v3.29 기준으로 지운다(홈 섹션명은 「내 웨딩 준비」).

**후기 별점 — 유지로 확정(2026-09-25 대표 결정 「9/23 결정으로」).** 아래는 경위다. v3.29 README.md가 「후기는
별점 없이 3축 3지선다」라고 적는데, 이건 **같은 날(2026-09-23) 앞서 대표님이 직접
확인하신 「후기 별점 UI를 되살린다」와 정반대다** — 그 결정으로 PR #497이 검색
업체상세·Pick 후보카드·MY 후기목록에 별점을 이미 복원했고 KakaoCloud API(run #1171)까지
배포됐다. **v3.29를 문자 그대로 따라 별점을 다시 지우는 작업을 지금 하지 않는다** —
`canonical-manifest.json`의 `openQuestions`에 이 충돌을 적어 뒀다. 대표님이 어느 쪽인지
확인해주면 그때 반영한다 — **2026-09-25에 확인받았다: 별점 유지.** 후기 화면 셋은 별점을 그대로 둔다.

v3.29가 바꾼 것(근거: `docs/design/CHANGELOG.md`) — 별점 항목만 위 경고를 따른다:

| 자리 | v3.29 |
| --- | --- |
| 핵심 메시지 | 「플래너 없이, 직접 고르는 웨딩 준비」로 확정. 추천·TOP3·결정사 흔적 삭제 |
| 후기 | ~~별점 없이 3축 3지선다.~~ **별점 유지로 확정(2026-09-25 대표 결정)** |
| 로그인 | 타이틀·혜택 4줄 교체, 만 14세 체크·약관 문구를 약관 동의 화면으로 일원화 |
| 약관 동의 | 한 화면 통합(WP-AUTH-010) — 필수 5 · 선택 3 · 앱 접근 권한 4칸. Pick 인증·상담 녹음 필수 |
| 약관 상세 | 공통 풀팝업 신설(WP-AUTH-011) — 탭 6개, 전 조문 작성 |
| 풀팝업 | 헤더 56px · 좌우 16px · 좌측 36px 슬롯 회색 원형 X + 중앙 타이틀 |
| 일반 화면 | 헤더 56px · 좌측 36px 슬롯 뒤로가기(24px 아이콘, 36px 터치영역) |
| 바텀시트 | **타이틀 + 우측 X 닫기** |
| 홈 「내 웨딩 준비」 아이콘 | Material Symbols Outlined 4개(`storefront`·`face_retouching_natural`·`dry_cleaning`·`diamond`, 20px), 구현 시 교체 가능 |
| 화면 번호 | 홈 파일을 앱 흐름 순서로 재배치, 1~17 재정렬, 가로 행 배치 |
| 캔버스 | 앱 목업 **375 × 812** |

- Root 탭은 홈 · 검색 · Pick · 웨딩노트 · MY. 검색 Root의 Back은 없고 삭제·통합된 독립 페이지를 되살리지 않는다.
- 좌우 여백 24 · 코랄 브랜드 토큰(스킨 6종: Coral 기본·Red·Yellow·Green·Blue·Dark Gray) · SEED 기준. Pick Mark 두 path는 절대 변경 금지다(`docs/design/PROJECT_RULES.md`에 확정본 — 기존 값과 동일, 안 바뀜). 로그인 UI는 카카오 단일 기준이다.
- 화면당 Primary CTA 1개. Pick이 가장 중요한 행동, 비교는 보조. 섹션 제목 1줄·서브카피 없음, 안내 최대 2줄·CTA 1줄.
- **상담 예약은 Pick에 담은 업체면 된다 — 최종 결정이 먼저일 필요가 없다**(2026-09-25 대표 결정 A). 최종 결정 확인 시트(`/pick/confirm`)는 삭제했고, 서버도 `vendor_candidates`에 있는 업체면 상담 일정을 받는다. 예전 규칙 「최종 Pick의 서버 저장 성공 뒤에만 상담 예약」은 폐기.
- 등록·추가·작성은 BottomSheet 기준을 적용한다. 인증 후 Back으로 로그인에 역진입하지 않는다.
- **등록 버튼은 헤더 영역에 있는 것만 쓴다 — 전체 UX 통일이다**(2026-09-23 대표 지시). 화면
  본문(리스트 행·카드 안·플로팅 버튼 등)에 별도 등록·추가 진입점을 세션이 임의로 만들지
  않는다. 등록·추가 CTA는 헤더 우측(`headAdd`류)에 하나만 둔다. `.dc.html`에 본문 안
  등록·추가 링크가 이미 있어도 그대로 베끼지 말고 헤더로 옮기는 것이 맞는지 먼저 확인한다
  — 정본을 근거로 새 진입점을 만드는 것도 **임의 추가**에 해당할 수 있다. 애매하면
  만들지 않고 `DESIGN_UNRESOLVED`로 남긴다.
- 헤더·탭바·칩바가 콘텐츠와 겹치지 않게 한다. 가로 탭은 압축 대신 한 줄 + 가로 스크롤, 세로 콘텐츠는 탭바·Safe Area에 가리지 않게 bottom padding을 확보한다. **화면별 임시 패치가 아니라 공통 컴포넌트 단위로 고친다.**
- `.dc.html`은 **디자인 레퍼런스**다. `x-dc`·`sc-for`·`sc-if`는 그 도구 전용 문법이라 무시하고 렌더된 구조와 문구만 가져온다. **문구(카피)는 그대로 쓰고 임의로 바꾸지 않는다.**
- `spec/`는 런타임 구성이다. 정본을 대체하지 않고 승인된 값을 공용 토큰·문구에 반영한다. 새 값의 근거를 기록한다.
- 전달 ZIP 자체는 저장소에 두지 않는다(2026-09-22 대표 지시 「이것만 올려」, 2026-09-23에도 동일 패턴). ZIP 해시와 푼 파일 20개의 해시는 `canonical-manifest.json`에 있고 `npm run test:design-canonical`이 센다.
- **정본(v3.29)에 없는 기능은 제거한다**(2026-09-23 대표 지시 — 「정본과 다른 기능은 제거한다」, v3.28에서도 같은 지시였고 이어진다). 1:1 대조는 값을 정본에 맞추는 데서 끝나지 않는다 — 코드에는 있는데 16개 `.dc.html` 어디에도 없는 화면·버튼·기능은 「못 맞춘 값」이 아니라 **삭제 대상**이다. **범위는 화면 단위가 아니라 화면 «안» 요소까지다**(2026-09-23 대표님 확인 — 「화면 안 요소까지 폭넓게」). 통째로 없는 화면·라우트만이 아니라, 남는 화면 안의 버튼·토글·옵션·필터 하나하나도 정본 대조표에 없으면 지운다. 다만 곧바로 지우지 않는다: 공용 컴포넌트인지, 다른 화면군(다른 세션 담당)이 같이 쓰는 라우트인지 먼저 확인하고, 이미 이 파일에 예외로 못 박힌 것(지도·달력 외부 연결 등)은 제외한다. 무엇을 왜 지웠는지 PR 본문에 남긴다.
- **대조는 「비슷하게」가 아니라 토큰·픽셀 단위다**(2026-09-23 대표 재지시 — 「토큰, 픽셀 단위로 대조해서 화면ID 하나하나 다 변경해」). 색상·간격·폰트 크기는 `spec/tokens.json`의 토큰 이름으로, 좌표·크기는 `.dc.html`에 박힌 px 값으로 맞춘다 — 「느낌이 비슷하다」·「대충 맞다」는 완료가 아니다. 화면ID(`WP-XXX-000`) 단위로 하나씩 열어 토큰값까지 대조하고, 다 됐다고 보고하기 전에 실제 스크린샷(`docs/screen-capture.md`)으로 나란히 놓고 픽셀 차이를 눈으로 확인한다. v3.29 이전 버전 잔존값(문구·색상·레이아웃·구 화면번호)은 발견하는 즉시 지운다 — 「일단 두고 나중에」 없다.
- **웨딩노트 `[open]` 2건은 2026-09-23(v3.28 시점)에 결정됐고 v3.29에도 유효하다.**
  - **상담 녹음 업로드 → 자동 정리: 신규로 추가한다.** Figma 원안대로 채택 — `packages/domain/src/visit-note-audio.ts` · `apps/api/src/analysis/gemini-visit-note-reader.ts` · `apps/mobile/src/app/(tabs)/wedding/[id]/visit-notes.tsx`가 이미 그 기반이다. 웨딩노트 화면에 진입점이 빠져 있다면 그것만 잇는다.
  - **상담에서 나온 금액은 예산에 합쳐 넣되, 배지로 출처를 다르게 표시한다.** 실 제보와 분리하지 않는다 — 총액·막대 그래프에는 함께 들어간다. `packages/domain/src/expense.ts`의 `EXPENSE_SOURCES`(`payment_proof` · `manual`)와 같은 자리에 `consultation`을 셋째 값으로 추가하고 `EXPENSE_SOURCE_LABEL`에 짧은 라벨(예: 「상담 정리」)을 단다 — 그 파일의 기존 규칙 그대로다: 「내가 쓴 돈이지 시장 자료가 아니다. 섞어도 된다 — 다만 어디서 온 값인지는 줄마다 적는다.」
- **관리자 시안이 크게 늘었다** — 옛 「약관 방침(관리자 편집 도구)」 하나에서 「웨딩픽 관리자」·「웨딩픽 관리자 운영」·「웨딩픽 약관 방침」 셋으로 나뉘었다. 아직 화면별 1:1 대조를 안 했다 — 관리자 화면을 고치기 전에 이 세 파일부터 읽는다.
- **랜딩·IA·컴포넌트 시트·디바이스 대응·사용자 흐름·스토어 이미지는 이번에 처음 정본으로 들어온 참고 자료다.** 특히 `웨딩픽 사용자 흐름.dc.html`은 v3.29 README 자신이 「옛 파일 링크가 남아 있어 재작성 필요」라고 적어 뒀다 — 그대로 근거로 쓰지 말고 최신 대메뉴 파일 기준으로 교차 확인한다.

## 화면 작업 절차 — 비교표 제출 전 수정 금지(2026-09-23 대표 지시)

오늘 실제로 사고가 났다 — 웨딩노트 화면을 「정본 기준으로 맞췄다」며 스크린샷까지 보여줬는데,
실제로는 잔가지 수정 하나(탭 이름·헤더 X버튼)만 반영된 옛 화면(월 달력 그대로)이었다. 진짜
개편(월 격자 폐기 → D-day 카드+주간 타임라인+체크리스트, WP-NOTE-001)은 다른 세션이 이미
만들어 뒀는데 병합 확인 없이 지나쳤다. **「디자인 기준으로 맞춰」라는 지시만으로는 이 사고를
막지 못한다** — 적당히 읽고 적당히 추론하게 된다. 대표님 지시로 아래 절차를 화면을 손대는
모든 작업(MASTER 포함)의 고정 명령으로 둔다.

**역할**: 화면을 만지는 세션은 구현자가 아니라 **디자인 검수자 겸 구현자**다. 이번 작업의
1순위는 「정본과 구현을 화면·컴포넌트 단위로 1:1 대조하고, 차이가 확인된 것만 고치는 것」이다.
추측·기존 구현 관성·임의 UX 개선을 금지한다.

**Source of Truth — 앱은 `docs/design/React_Native`, 관리자 · 랜딩은 `docs/design/html`이다**
(2026-09-24 대표 절대 지침 — 이 파일 맨 앞 ⛔). 앱 화면에 대해 `html/`의 대메뉴 시안과
`README.md` · `PROJECT_RULES.md` · `CHANGELOG.md`는 `React_Native`와 어긋나면 진다. 두 곳 어디에도
없는 화면은 `DESIGN_SOURCE_NOT_VERIFIED`다. **`docs/design/handoff/`·`docs/design/figma-export/`는 2026-09-22에 이미 파기됐고 이
저장소에 없다** — 다른 세션에 지시를 넘길 때 그 경로를 정본으로 적지 않는다. `.dc.html`은
**파일 이름만 보고 판단하지 않는다.** 해당 화면 ID(`WP-XXX-000`) 구역을 실제로 연다.

**값은 손으로 읽지 않고 실행해서 뽑는다(2026-09-23 신설, `scripts/canon/README.md`).**
`.dc.html`의 실제 style 값은 `<script>` 안에 문자열 결합(`'flex:1;...' + (on.a ? ... : ...)`)
으로 박혀 있다 — 오늘 사고가 정확히 이걸 손으로 안 풀어봐서 났다.

```
node scripts/canon/extract-style.mjs --file "docs/design/html/<파일>.dc.html" --wp WP-XXX-000
node scripts/canon/extract-rn-style.mjs --file "<구현 파일>" --key <스타일 키>
node scripts/canon/resolve-tokens.mjs --token Layout.cardPadding
```

첫째는 정본 값을, 둘째는 구현의 `StyleSheet.create` 값을(`packages/ui` 토큰까지 실제
숫자로 풀어서) 실행해서 뽑는다 — **이름이 같다고 값이 같다고 가정하지 않는다**
(`FontSize.tab`은 12, `LineHeight.tab`은 16이고 이 둘을 합친 `type="tab"`은 12px다 —
16px일 거라고 추측했다가 오늘 실제로 한 번 틀렸다). 일치·불일치 판정은 이 도구가 안
한다 — CSS와 RN StyleSheet는 속성 체계가 달라 기계 판정이 오히려 잘못된 확신을 준다.
사람이 표에 채워 넣는다.

**1. 코드를 고치기 전에 비교표부터 쓴다. 비교표 없이 코드를 고치는 것을 금지한다.**

| 항목 | 정본(`.dc.html` 경로 + WP-ID) | 구현(파일 경로 + 줄) | 일치 | 조치 |
| --- | --- | --- | --- | --- |
| Header | ... | ... | O/X | ... |

구조(Depth·Back·Tab 위치·CTA 위치·Fixed/Scroll 영역) · 스타일(여백·정렬·radius·색·타이포) ·
상태(기본·선택·미선택·Empty·Loading·Error·Disabled·완료) 셋 다 표에 넣는다. **기본 상태
하나만 맞추고 완료로 보고하지 않는다.**

**2. 추측 금지.** 「보통 이렇게 한다」·「이쪽이 UX상 낫다」·「기존 구현이 자연스럽다」·「아마 이런
의도」는 근거가 아니다. 정본에 없거나 불명확하면 만들지 않고 표에 **`DESIGN_UNRESOLVED`**로
남긴다. `.dc.html`을 못 읽거나 정본 자체를 확인 못 했으면 그 화면은 **`DESIGN_SOURCE_NOT_VERIFIED`**로
보고하고 임의로 진행하지 않는다.

**3. 증거 없는 「정본대로 수정 완료」를 금지한다.** 수정마다 정본 경로(`docs/design/html/...dc.html`
의 WP-ID)와 구현 경로(`apps/.../...tsx`) 쌍을 남긴다.

**4. 고친 뒤 같은 표를 다시 대조해 PASS/FAIL/NOT CHECKED로 채운다.** `NOT CHECKED`가 하나라도
있으면 완료로 보고하지 않는다.

**5. 실제로 렌더해서 본다.** `node scripts/screenshot-screens.mjs --build --route "<경로>"`로 찍고
정본과 나란히 놓는다(`docs/screen-capture.md`). 「코드상 동일할 것」이라고 렌더 없이 판정하지
않는다 — 2026-09-23 사고가 정확히 이 실수였다. 스크린샷을 보여주기 전에 **그 화면의 브랜치가
실제로 main에 반영됐는지, 다른 draft PR에 더 최신 구현이 있는지**부터 확인한다 — 오래된
브랜치·오래된 dist를 찍어 놓고 「반영됐다」고 보고하지 않는다.

**6. 최종 보고 순서를 고정한다.** 디자인 근거 → 기존 구현 → 불일치 → 수정 파일 → 실제 화면
검증(스크린샷) → 남은 불일치(`DESIGN_UNRESOLVED` 목록). 이 순서를 벗어난 「다 맞췄다」 요약만
있는 보고는 받지 않는다.

**공통 UI 규칙은 별도 지시 없이도 유지한다**: 등록·추가·작성은 BottomSheet 기준 · **등록
버튼은 헤더 영역에 있는 것만 쓴다(본문 안 별도 진입점 임의 추가 금지)** · 인증된
사용자는 Back으로 `/login`·`/setup`에 역진입 금지 · Overlay가 열려 있으면 Back은 Overlay만
닫음 · 2Depth 이상 Header 규격 통일 · Header 서브 문구 미사용 · 진입 출처에 따라 Back
destination 보존 · 정본에 없는 임의 Alert/Dialog 금지.

이 절차는 `docs/session-prompt.md`에도 같은 내용으로 들어간다 — 세션은 자기가 받은 지시부터
읽으므로 규칙이 여기만 있으면 소용없다.

규칙의 공유 기준은 최신 main이다. 기존 미커밋 작업을 보호하고 열린 PR의 소유 파일·충돌을 확인한다. 문서 변경도 PR 검증·병합 결과까지 확인하며 제품 구현·시각 검수·배포 완료와 구분한다. 세부 작업 원칙은 `AGENTS.md`를 따른다.

**지침(2026-09-10, 사용자) — 사람에게 말하는 모든 시각은 KST로 바꿔서 적는다.** GitHub Actions ·
이전 호스팅 · DB 로그는 전부 UTC로 나온다. 그 값을 그대로 옮기면 아홉 시간 어긋난 시각을 보고하게
되고, 「방금 배포됐다」와 「아침에 배포됐다」가 뒤바뀐다. UTC + 9 = KST이며, 한국은 서머타임을
쓰지 않아 계절과 무관하게 언제나 +9다. 애매하지 않게 `17:52 KST`처럼 시간대를 함께 적는다.

**저장하는 값과 cron은 UTC 그대로 둔다.** 바꾸는 것은 보고할 때뿐이다 — 워크플로의 cron은 UTC로
해석되고, DB의 timestamptz도 UTC로 담는다. 거기까지 KST로 바꾸면 스케줄이 아홉 시간 밀린다.

**지침(2026-09-10, 사용자) — 규칙 md는 `main`에 올린다. 모든 세션은 `main`을 기준으로 한다.**
이 파일과 `docs/`의 규칙 문서는 브랜치에 두고 끝내지 않는다. 브랜치에만 있는 규칙은 그 브랜치를
만든 세션 하나만 읽는다 — 다른 세션은 낡은 규칙을 현행으로 알고 일하고, 그 차이는 PR이 서로
어긋날 때가 되어서야 드러난다. 규칙을 고쳤으면 **그 PR을 먼저 머지해서 `main`에 올린다.**

세션 쪽의 의무도 같다. 일을 시작하기 전과 PR을 올리기 전에 **`main`을 브랜치에 머지한다.**
읽는 규칙도 `main`의 것이고, 기준으로 삼는 코드도 `main`이다. 오래 도는 브랜치일수록 먼저 맞춘다.

**지침(2026-09-10, 사용자) — 하위 세션은 작업을 마치면 PR을 건다.** 브랜치에 커밋해 두고 보고만
하는 것으로 끝내지 않는다. 브랜치는 그 세션 밖에서 보이지 않는다 — 누가 브랜치 이름을 알고
일부러 열어보지 않는 한, 끝난 일이 끝나지 않은 것과 똑같이 보인다.

오늘 실제로 그랬다. 「웹뷰 디자인 전담」이 사용자 오더 5건을 다 마치고 커밋까지 해뒀는데 PR이
없어서, MASTER가 브랜치를 직접 뒤져 보기 전까지 「아직 안 됐다」로 알고 있었다.

PR은 이렇게 건다.
- **초록으로 만든 뒤에 올린다** — typecheck · lint · 카피 린트 · jest. 빨간 PR은 검수를 못 받는다.
  **그 초록은 네 손으로 만든 초록이다** — 2026-09-15부터 브랜치와 PR에서는 CI가
  돌지 않는다(아래 「CI는 한 번만 돈다」). 무엇을 돌렸고 몇 건이 통과했는지를
  PR 본문에 적어라. 안 돌리고 「통과했다」고 적으면 그대로 통합 CI에서 터진다.
- **화면을 고쳤으면 찍어서 붙인다** — `node scripts/screenshot-screens.mjs --build --route "<경로>"`
  (`docs/screen-capture.md`). 코드와 시안을 눈으로 대조한 것은 **「본 것」이 아니다** — 2026-09-11에
  세션 셋이 그렇게 하고 검색 화면이 시안과 전혀 다른 채로 배포됐다.
- **피그마 시안도 같이 찍어 나란히 붙인다** — `node scripts/screenshot-figma.mjs`
  (2026-09-14에 생겼다). 「시안은 못 찍는다」는 `docs/design/handoff/`의 `.dc.html`에만
  해당한다(`_ds/` 번들과 `support.js`가 용량 때문에 저장소에 안 들어온다 — 2026-09-11
  대표님 확인). **피그마 저장소는 그냥 도는 Vite 앱이라 찍힌다.** 못 찍는 줄 알고 사람
  눈에 맡긴 동안 홈 · 검색 · Pick이 통째로 어긋나 있었고, 2026-09-14에 대표님이 앱을
  열어 보시고서야 드러났다. **화면군 기준 크기를 맞춰 찍는다**(앱 390×844·관리자 1920×1080) — 폭이 다르면 없는 차이가
  보이고 있는 차이가 묻힌다.
- **`main`을 먼저 머지한다.** base와 벌어진 PR은 리뷰가 아니라 충돌 해결부터 시작한다.
- 본문에 **무엇이 어떻게 달랐고 무엇으로 고쳤는지**를 적는다. 못 고친 것과 판단이 필요한 것은
  따로 적는다 — 「판단 필요」를 조용히 「완료」에 섞지 않는다.
- 초안(draft)으로 올려도 된다. 머지 여부는 MASTER가 정한다.

**MASTER 쪽 의무도 같다.** 전담 세션을 만들거나 일을 배정할 때 **「작업을 마치면 PR을 건다」를
지시에 반드시 넣는다.** 규칙이 `main`에 있어도 세션은 자기가 받은 지시부터 읽는다 — 지시에
없으면 브랜치에 커밋해 두고 보고로 끝낸다. 배정한 뒤에는 PR이 실제로 올라왔는지 확인한다.
커밋만 쌓여 있고 PR이 없으면 그것은 「진행 중」이 아니라 **보이지 않는 완료**다.

전담 배정·독립 반례 검수·완료 세션 흡수와 정리는 `AGENTS.md`의 마스터 운영 규칙을 따른다.
결과와 미해결 항목을 `docs/sync/master-status.json`에 인수한 뒤 완료된 세션을 보관한다.

**지침(2026-09-11, 사용자) — 클로드 작업과 겹치면 사용자가 지정한 Codex 마스터가 수정·검수한 변경을 우선한다.**
해당 마스터는 `docs/sync/master-status.json`의 `masterThreadId`로 식별한다. 겹치지 않는 상대 변경은
보존하며 브랜치 전체를 덮어쓰지 않는다. 충돌 해결 사유, 양쪽 PR·검토 SHA, 선택·보존한 변경,
통합 검증 결과와 미해결 항목을 PR 및 같은 상태 파일에 인계한다. 우선권은 미검증 자동 병합의
근거가 아닌 검수된 변경 간 충돌 해결 기준이다. 최신 사용자 지시·정책과 원래 디자인 기준,
마스터의 독립 검수와 관련 통합 검증을 그대로 적용한다.
사용자가 우선순위를 명시적으로 바꾸면 관련 규칙·전달문을 함께 갱신하고 같은 작업에서 PR로
main에 반영한 뒤 병합 결과를 확인한다. 세부 기준은 `AGENTS.md`의 동시 작업 충돌 규칙을 따른다.

## v3.18 용어 통일 — 확인된 제보 → 실 제보

정보 체계 3단: 상위 개념 **실 제보** · 금액 **제보 금액** · 제공 행동 **Pick 인증**.
금액 표기는 `152~184만원` / `실 제보 12건 · 최근 12개월 · 기준금액 168만원`, 기준금액 설명은
「실 제보의 중앙값이에요」. 코드 치환 규칙 `"확인된 제보" → "실 제보"` — `grep -r "확인된 제보"`가
0이어야 한다(금지어 목록을 적은 줄만 예외). `확인된 정보`도 같은 자리라 같이 바꾼다.

---

## v3.27 관리자(2026-09-10)
- 관리자 콘솔 기준 해상도가 1440×900 → **1920×1080**이다. 사이드바 240 + 본문. 감사 기록처럼
  컬럼이 많은 표가 1440에서는 가로 스크롤 없이 들어가지 않았다.
- 시안이 없던 관리자 화면 **11개**가 새로 생겼다: WP-ADM-002 일일 브리핑 · 013 이상치·조작 탐지 ·
  015 이미지 자동 수급 · 016 이메일 회신 자동 매칭 · 030 마케팅 자동화 · 032 수익 현황 ·
  040 자동화 상태 · 041 긴급 중지 · 042 변경 복구 관리 · 051 정책 규칙 관리 · 052 감사 기록.
- 관리자 공통 규칙 넷: 지금 봐야 할 것이 맨 위(상단 배너가 상태를 먼저 말함) · 빈 상태가 정상
  상태(「확인할 것이 없어요」를 반드시 그림) · 표는 카드 안에서만 스크롤 · 위험한 조작은 무엇이
  바뀌는지 항목으로 보여준 뒤 한 번 더 확인.

## v3.24 구현 원칙(2026-09-08)
- 정보 0층: 실 제보 3건 미만이면 `업체 안내 N만원~`(회색 · 출처)로 대신, 둘 다 없으면 `수집 중` + Pick 인증 CTA.
  금액 한 줄은 어느 화면이든 `priceLine(paidPrice, guidePrice)`(packages/domain guide-price.ts)로만 만든다.
- 스타일은 `WeddingStyle`(URBAN · NATURAL · ROMANTIC · GLAMOROUS) 넷뿐. 화면 라벨은 도시적인 · 자연스러운 · 로맨틱한 · 화려한.
  최소 1개, 최대 2개. 3번째 선택은 추가하지 않고 정본 토스트를 표시한다. 태그는 정렬 가중치로만 쓴다 — 태그가 다르다고 업체를 빼지 않는다.
- 홈 상태는 2층(진행 0 · 1~8 · 9+ / 정보량 3건 기준)이다. 준비 현황은 홈 4칸 + WP-HOME-009 두 곳뿐.
  코랄은 홈에서 다섯 곳 — 현재 업종 테두리 · 진행바 · 웨딩픽 추천 라벨 · CTA · D-day(2026-09-09 사용자 오더).
- 검색은 온보딩 값을 미리 걸지 않는다. 알림은 진행 중 업종만 · 하루 2건. 날짜 선택은 **OS 날짜 선택기**, 시간 선택은 **OS 타임피커**(2026-09-25 대표 지시 — 바텀시트에서 「OS 데이트피커」 · 「시간 선택 타임피커」). WP-APP-023 휠 3열(2026-09-11)은 폐기.
- Npay 수령(WP-EVT-006): 휴대폰 번호는 보내는 데만 쓰고 보낸 뒤 지운다(reward_payouts CHECK). 돈은 사람이 보내고
  `reward-admin --payouts / --sent / --payout-failed`로 사실만 적는다. 본인에게도 번호 전체를 되돌려주지 않는다.

## 심볼 — 절대 변경 금지
Pick Mark는 하트 안에 체크. 아래 두 path가 확정본이며 어떤 이유로도 바꾸지 않는다.

```
<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">
  <path d="M12 20.5S3.5 15.2 3.5 9.9A4.4 4.4 0 0 1 12 8.1a4.4 4.4 0 0 1 8.5 1.8c0 5.3-8.5 10.6-8.5 10.6Z"></path>
  <path d="M9.4 11.9l1.7 1.7 3.4-3.4"></path>
</svg>
```

- 체크는 하트 중앙 배치. 가로 중심 12.0, 세로 중심 11.9.
- 코드의 원본은 `spec/tokens.json` `symbol` 블록이고 위 path와 같다(2026-09-08 핸드오프판으로 통일).
- 적용처: 앱 아이콘 5종 · 스플래시 · 시작 화면 · Pick 탭 · Pick 완료 모션 · 웹 GNB · 관리자 로그인/사이드바.
- 앱 아이콘과 스플래시는 스킨 컬러와 무관하게 브랜드 키 컬러 `#FF6F61` 고정, 마크는 `#FFFFFF`.
  **2026-09-15 대표 지시로 코랄 정본으로 돌아왔다** — 「기존 정본색상으로 싹다 다시 바꿔.
  코랄색으로」. 2026-09-14 하루 동안 `#E7898D` · 마크 `#371B34`였고, 되돌린 값은 전부
  코랄판(`8a259608~1`)에서 그대로 읽어 왔다 — 지어낸 값이 없다.

  **「스킨과 무관하게 고정」이라는 성격은 그대로다.** 이 줄이 막는 것은 사용자가 고른 스킨이
  아이콘까지 따라가는 것이고, 그 위험은 스킨 6종이 남아 있는 한 그대로다.

  **흰 마크는 `#FF6F61` 위 3.03:1이다.** WCAG AA 본문 4.5:1에는 못 미치고 큰 글자 3.0:1은
  넘는다. Pick Mark가 stroke 1.9의 가는 선이라 29·48px에서 하트 안 체크가 뭉개질 수 있다 —
  코랄판이 원래 그랬고 대표님이 그 판으로 돌아가라고 하셨다. 값의 원본은
  `spec/tokens.json` `symbol.appIcon`이다.

**운영 서버의 모델 호출은 전부 제미나이다. 클로드 API는 전면 폐기했다**(2026-09-15
대표 지시 — 「클로드 API는 싹다 전면 폐기하고 제미나이로 명시해」 · 「결제 증빙·분석 /
웨딩피드 / 녹음파일 분석 다 제미나이다」).

| 쓰는 곳 | 하는 일 |
| --- | --- |
| `analysis/gemini-analyzer.ts` | 견적서 · 계약서를 읽어 구조화 |
| `analysis/gemini-payment-reader.ts` | 결제 증빙 OCR |
| `analysis/wedding-feed-writer.ts` | 웨딩피드 글쓰기 |
| `analysis/consultation-reader.ts` | 상담 녹음 읽기(Files API) |
| `analysis/gemini-visit-note-reader.ts` | 상담 녹음에서 방문노트 네 칸 |

모델 이름은 `config.geminiModel` **한 칸**이다. 다섯이 같은 칸을 본다 —
`analysisModel`(클로드 이름을 담던 칸)은 없앴다.

**Gemini는 이 세 가지 일만 한다**(2026-09-23 대표 지시 — 「제미나이 api는 사진, 이미지
분석 및 정보 추출 / 녹음 파일 정보 추출 / 관리자 피드 자동생성 말고는 절대 사용 금지한다」).

1. 사진 · 이미지 분석 및 정보 추출(견적서·계약서·결제 증빙)
2. 녹음 파일 정보 추출(상담 녹음)
3. 관리자 피드 자동생성(웨딩피드)

위 다섯 파일이 그 전부다. **웹 검색 · 텍스트 생성 · 그 밖의 어떤 용도로도 새로
부르지 않는다** — "정보 추출의 일종"이라며 넓히지 않는다.

**공공 통계는 3번(웨딩피드) 안에서만 넘긴다**(2026-09-24 대표 지시 — 「피드에는 써도
된다. 진행해」). Gemini가 공공 API를 부르지 않는다 — 서버가 받아 둔 값
(`structured.public_stats`, 0431)을 `wedding-feed-writer.ts`에 주제와 함께 건넬 뿐이라
부르는 파일은 늘지 않는다. 본문 숫자가 넘긴 숫자와 하나라도 다르면 글을 버리고
(`findUnlistedNumbers`), 출처 줄은 서버가 붙인다(`statSourceLine`). 피드 밖(화면 문구 ·
요약 · 분류)에서 공공 자료를 Gemini로 가공하지 않는다.

**오늘 이미 하나가 이 규칙 밖이었다.** `expo-collector.ts`의
`createGeminiExpoDiscoverer`가 Google Search 그라운딩으로 웹을 검색해 박람회
후보를 짓고 있었다 — 이미지도 녹음도 피드 생성도 아니다. 이 지시가 나온 자리에서
지웠다: 함수·프롬프트·`worker-loops.ts`의 자동 수집 루프·`POST
/v1/admin/expos/collect`(전부 「대체 출처 미구현」으로 잠금)까지 걷어냈다. **박람회
신규 자동 수집은 지금 꺼져 있다** — 공공데이터 출처(예: 한국관광공사 TourAPI)로
다시 만들기 전까지는 관리자가 직접 등록한다. `runExpoCollection`·`ExpoDiscoverer`
타입은 출처를 가리지 않게 남겨 뒀으니 그 자리에 새 수집기를 잇는다.

**시험이 막는다.** `apps/api/src/analysis/gemini-scope.test.ts`가 `callGemini`를
부르는 파일이 위 다섯과 정확히 같은지 센다 — `no-claude.test.ts`와 같은 이유다.
글로 적은 규칙은 다음 세션이 조용히 넓힐 수 있지만 세는 시험은 파일 목록을 못 박는다.

**이 자리가 2026-09-15 하루에 두 번 뒤집혔다. 왜 그랬는지를 적어 둔다.**

아침에 클로드로 옮겼다 — 「제미나이는 녹음파일 인식, OCR 확인 외 절대 사용금지다」.
그 지시의 이유는 돈이었다: 「클로드는 Max 구독 안에서 처리되고 제미나이는 구글 계정으로
따로 청구된다」.

**그 전제가 틀렸다.** Max 구독이 덮는 것은 사람이 쓰는 Claude Code 세션이고,
**운영 서버가 `ANTHROPIC_API_KEY`로 API를 부르는 것은 구독 밖이라 종량 과금이다.**
옮긴 것이 돈을 아낀 것이 아니었고, `gemini-2.5-flash-lite`가 그 일에 훨씬 싸다.

대표님이 먼저 짚으셨다 — 「엔트로픽 api key면 요금 발생하는거 아니냐?」. 그 한 줄이
아침 지시의 근거를 무너뜨렸다.

**전제를 확인하지 않고 규칙을 적으면 이렇게 된다.** 아침에 「돈 때문에 클로드」라고
적을 때 아무도 「그 클로드는 어느 클로드인가」를 묻지 않았다 — 세션의 클로드와 서버가
부르는 클로드는 값이 다른데 같은 이름이라 같은 것으로 읽혔다.

**되돌아가는 것을 시험이 막는다.** `apps/api/src/analysis/no-claude.test.ts`가 저장소
전체에 `@anthropic-ai/sdk`와 `ANTHROPIC_API_KEY`가 한 줄도 없는지 센다. 글로 적은
규칙은 깨지지만 세는 시험은 안 깨진다 — 오늘 하루에 부르는 코드 · 시험 · 배포 선언이
따로 놀았다.

`@anthropic-ai/sdk` 의존성도 `apps/api/package.json`에서 뺐다.

## 용어
- 사용자 화면에 `AI` 금지. `웨딩픽이 골랐어요` · `웨딩픽 TOP3` · `웨딩픽 콘텐츠`.
  **관리자 화면에서도 쓰지 않는다**(2026-09-11 대표 지시 — 「전체 메뉴명과 세부 명칭은
  AI식 단어를 쓰지 않고, 한국식 토스식 용어를 사용한다」). 2026-09-04판의 「관리자는
  AI 용어 유지」를 뒤집은 결정이다. 메뉴 이름과 화면 안 명칭이 대상이고, **내부 개발
  문서와 비용 관리 문서에서는 그대로 쓴다** — 거기서 「AI」를 빼면 무엇을 부르는 말인지
  사라진다. 화면 쪽은 짧고 명사로 끝나는 말로 바꾼다.
- 사용자 화면에 `데이터` 금지. `실 제보` · `실 제보 N건` · `많이 확인된 곳` · `아직 정보가 적어요`.

**사용자 화면에 영문을 쓰지 않는다**(2026-09-15 대표 지시 — 「이딴 영문 싹다 없애」).
남는 것은 **대표님이 따로 정하신 이름 둘뿐**이다 — `Pick` · `Npay`.

**피그마의 영문 eyebrow는 한국어로 바꾸는 것이 아니라 «지운다»**(2026-09-15 대표 지시 —
「위와 같이 온보딩, 전체 메뉴에 이런 형식에 맞지 않는 화면 있으면 싹다 찾아서 삭제해」).
제목 위에 영문 소문자 간격을 넓혀 얹은 **장식 줄**이고, 한국어로 옮기면 그 장식이
어색해진다. **줄째 없앤다.**

피그마가 그렇게 그린 자리 열셋이 `docs/rn-migration/FIGMA_DESIGN_SYSTEM.md:87`에 있다:

    WEDDING, LESS OVERWHELMING · JUST FOR YOU · VERIFY THE FACTS · MY NOTES ·
    TODAY'S PICK · UPCOMING FAIR · RELATED · OUR CALENDAR · BUDGET OVERVIEW ·
    CONSULTATION · YOUR CONSULTANT · BLOOMING STUDIO · VERIFIED PRICE RANGE

**아직 안 만든 화면을 만들 때도 이 줄을 넣지 않는다.** 규격서에 있어도 넣지 않는다.

eyebrow가 아닌 영문(버튼 · 라벨 · 안내문)은 **한국어로 바꾼다.** 값은
`spec/strings.ko.json`에서 가져오고 화면에 글자를 직접 적지 않는다.

**날짜 표기 `D-127`은 이 규칙의 대상이 아니다** — 영문 단어가 아니라 한국에서 쓰는
날짜 세는 꼴이다. 대표님이 따로 말씀하시면 그때 바꾼다.
- 정보 체계 3단. 상위 개념 `실 제보` · 금액 `제보 금액` · 제공 행동 `Pick 인증`.
  `확인된 제보` `확인된 정보` `확인된 금액` `실제로 낸 금액` `실제 결제` `실제 결제 데이터`는 쓰지 않는다.
- 애매모호 표현 금지: `거의` `아마` `대략` `어느 정도` `가능성이 높음` `~일 것으로 보임` 근거 없는 `약`·`정도`.
  대신 `확정` `미확정` `검증 전` `확인 필요` `조건부` `측정값` `추정값`.
- `탐색` 금지 → `검색`. `관심업체` 폐기 → `Pick`.

**지침(2026-09-11, 대표) — 같은 것을 두 이름으로 부르지 않는다. 업종도 지역도 마찬가지다.**
한 업종·한 지역에 이름은 하나뿐이다. **가장 많이 쓰인 쪽으로 통일한다** — 어느 쪽이 많은지는
세어서 정하고, 센 결과를 근거로 남긴다. 시안 한 장에서 다르게 적혔다고 공용 이름을 바꾸지
않는다. 이름이 갈리면 같은 목록이 두 줄로 뜨고, 사용자는 그것을 서로 다른 것으로 읽는다.

세어서 정한 것(루트 시안 36장 · 2026-09-11):

| 키 | 통일한 이름 | 센 값 | 비고 |
| --- | --- | --- | --- |
| `snap` | **본식스냅** | 본식스냅 14 · 스냅 6 | 검색 칩의 「스냅」은 칩 폭 때문에 줄여 적은 것 |
| `hair` | **헤어변형** | 헤어변형 6 | 「헤메」 11은 업종이 아니라 **가짜 업체 이름**이다(「청담 D 헤메」) |
| ~~`wedding_info_company`~~ | ~~결정사~~ | — | **2026-09-24 대표 지시로 제거** — 「웨딩픽은 플래너 없이 누구나 예약 가능한 웨딩 플랫폼이다. 고로 결정사 따윈 필요없다」. 업종 선택지·준비 현황·기본 할 일·예산 묶음·상담 분류·피드 주제에서 뺐다. DB enum 값만 과거 기록용으로 남는다(`STORED_VENDOR_CATEGORIES` · 고르는 목록은 `VENDOR_CATEGORIES`) |

**지역은 짧은 꼴 아홉으로 고정이다** — 서울 · 경기 · 인천 · 부산 · 대구 · 대전 · 광주 · 울산 ·
그 외(`WEDDING_REGIONS`). 루트 시안도 같고 긴 꼴은 한 번도 안 나온다. **긴 꼴은 들어오는
자리에서 떼어낸다** — `REGION_SUFFIX_PATTERN`을 SQL과 코드가 함께 쓴다. 한쪽만 고치면
필터가 갈리고, 2026-09-10에 실제로 「경기」와 「경기도」가 나란히 떴다.

수를 세는 것은 **이름이 여럿일 때 어느 쪽으로 갈지**를 정하는 방법이지, 업종이 무엇인지를
정하는 방법이 아니다. 위 표의 「헤메」와 「플래너」가 그 예다 — 둘 다 수는 많지만 업종 이름이
아니라서 후보가 아니었다. **세기 전에 그것이 같은 자리를 가리키는 이름인지부터 본다.**
- v3.22 홈 · 혜택 용어: `네이버페이 포인트` → `Npay`(NPay · 네이버 페이 표기도 금지), `우리 준비` → `준비 현황`,
  `오늘의 Pick` → `웨딩픽 추천`. `grep -r`로 잔존 0이어야 한다(금지어 목록을 적은 줄만 예외).
- 혜택 화면에 운영 기간을 적지 않는다: `하루 안에` `매달` `~까지` `30일 안에` `마감일` `당첨 확률` 금지.
  「확인 후 알려드려요」 「새 회차가 열리면 알려드려요」로 쓴다. 완료된 기록의 날짜(8월 21일 완료)는 남긴다.
  `~하지 않아요` `~못해요` `~안 해도`로 끝나는 안내 금지 — 무엇이 되는지를 말한다.
- 혜택 안내 시트(WP-SHT-017): 온보딩 완료 후 홈 최초 진입 1회 · 400ms 뒤. 닫으면 `sheetSeen` 저장. 남은 응모 조건
  2개 「두 가지만 더 하면」 · 1개 「하나만 더 하면」 · 0개면 시트 대신 응모 완료 알림. Npay 로고는 CSS로 그린다.

## 디자인

현재 기준은 이 파일 맨 앞의 「현재 디자인 기준 — v3.28」과 `docs/design/README.md`를 따른다. 이 문서에 서체·여백·Root 탭·카피의 이전 기준을 복제하지 않는다.

숫자는 실제 금액·날짜·건수 의미를 유지하고 공용 한국어 포맷 함수를 사용한다. 홈 서브문구의 불필요한 숫자 배제 요청을 실제 수치 삭제로 확대하지 않는다.

## 예시 데이터
- 웹은 가명 업체(`강남 A 스튜디오` 형식)와 샘플 이미지를 유지한다. 실제 상호와 실제 촬영물을 노출하지 않는다.
- 앱은 실제 업체명과 실제 이미지를 쓴다. 시안의 가명은 개발 시 실제 데이터로 대체한다.
- 금액은 웹·앱 모두 실 제보로 집계한 실제 구간을 쓴다.

## 세션에 그대로 넘기는 프롬프트

전담 세션을 만들 때 붙여넣을 전문이 `docs/session-prompt.md`에 있다. Codex에 넘길 때는
같은 `docs/session-prompt.md`를 사용하고 역할·이번 작업 범위를 명시한다. 도구별 규칙 사본을 만들지 않는다.
**규칙을 고치면 세 파일(이 파일 · 두 전달문)을 함께 고친다.** **세션은 자기가 받은
지시부터 읽는다** — 규칙이 여기 있어도 지시에 없으면 브랜치에 커밋해 두고 보고로 끝낸다.
그래서 규칙을 지시에 통째로 넣는다. 규칙을 고치면 그 파일도 같이 고친다.

## 매번 지키는 것 — 짧게 적어 두는 자리

**호칭은 대표님이다**(2026-09-10 사용자 지시).

**전달 ZIP과 시각 원본은 보존한다.** 활성 MD는 현재 확정 기준으로 유지하고, 사용자 요청에 따른 변경·삭제는 manifest에 근거와 해시를 기록한다. 문서 정리를 시안·토큰의 임의 재설계 근거로 쓰지 않는다.

**prettier를 돌리지 않는다.** 이 저장소의 서식은 손으로 정한 것이고, 한 번 돌리면 관계
없는 파일 수백 개가 diff에 들어와 무엇을 고쳤는지가 사라진다.

**로딩과 기본 상태는 실행 핸드오프 §8·§8-1을 따른다.** 로그인은 기본 로더 위·문구 아래로 한 덩어리로 표시하며 문구는 “카카오로 로그인하는 중이에요”다. 목록·상세·폼 skeleton은 실제 화면 구조를 따른다. 조회 실패와 빈 결과를 섞지 않는다.

**Root 탭과 온보딩은 현재 명세를 따른다.** 탭의 공용 구현은 `root-tabs.ts`, 온보딩 순서는 `flow.ts`로 모은다. 날짜·지역·준비 현황·예산·스타일 다섯 질문을 기준으로 검수한다.

**보이는 이름과 라우트를 함께 바꾸지 않는다.** `/wedding`·`/community` 등 저장된 링크는 통합·리다이렉트 규칙으로 보존한다. Pick Mark와 현재 시안의 탭 형태를 따른다.

**온보딩 결과는 확인 화면이다.** 사용자가 완료를 누를 때 저장하고 저장 성공 뒤에 완료 처리한다. 재진입·캐시·Back에서 입력·인증 상태를 보존한다.

**뒤로 가기 단추의 자리는 상세 화면끼리 같다.** 한 화면만 다르면 그 화면이 고장 난 것처럼
보인다.

**앱 밖으로 나가지 않는다 — 바깥 주소는 전부 앱 «안»에서 연다**(2026-09-15 대표 지시 —
「인앱에서 웹 새창 또는 이동 시 앱을 탈출하게 된다. 하여 iframe 껍데기 씌워서 웨딩픽 앱
밖으로 나가지 못하게 한다」 · 「서비스이용약관 개인정보처리방침 약관 관련된것도 다
In-App Browser 형태로 변경한다」).

지금은 반대다. `features/open-external.ts`가 **웹에서 `window.open(_blank)`**, 네이티브에서
`Linking.openURL`을 부른다 — 둘 다 앱을 떠난다. 약관 · 방침이 그 함수를 쓴다
(`login/index.tsx` · `my/support.tsx` · `my/policies.tsx` · `my/privacy.tsx`).

    네이티브(iOS · 안드로이드)   `expo-web-browser`의 인앱 브라우저로 연다.
                                시스템 시트가 앱 «위에» 뜨고 닫으면 앱으로 돌아온다.
    웹 빌드                      우리 화면 안의 iframe 껍데기로 연다. 닫기는 우리가 그린다.

`expo-web-browser`는 이미 들어 있다 — 로그인이 쓴다(`features/auth/providers.ts`).
새로 넣을 것이 없고 **부르는 함수만 바꾼다.**

**우리 사이트는 iframe에 들어간다 — 재서 확인했다**(2026-09-15).
`210.109.82.212` · `210.109.82.212` 둘 다 응답에
`X-Frame-Options`도 CSP `frame-ancestors`도 없다. 약관 · 방침은 우리 사이트가 정본이므로
껍데기로 감싸는 데 걸리는 것이 없다. **그 헤더를 나중에 켜면 약관 화면이 빈 칸이 된다** —
보안 헤더를 더할 때 이 줄을 같이 본다.

**남의 사이트는 대부분 iframe을 거절한다.** `X-Frame-Options: DENY`나 CSP
`frame-ancestors`를 걸어 두기 때문이고 **우리가 못 바꾼다.** 박람회 신청 페이지 · 네이버 ·
인스타그램이 여기에 해당할 수 있다. 그래서 남의 주소는 **네이티브는 인앱 브라우저**(막히지
않는다), **웹은 iframe을 시도하고 안 뜨면 새 탭으로 넘기며 「새 창에서 열려요」를 알린다.**
빈 칸을 보여주고 끝내지 않는다.

**지도와 달력은 이 규칙의 예외다.** `map.kakao.com`(업체 상세 · 웨딩 지도)과 `.ics` 내려받기
(`search/expo/[expoId]/calendar.tsx`)는 **다른 앱에 넘기는 자리**다. 지도 앱에서 길을 찾고
달력 앱에 일정을 넣는 것이 목적이라 앱 안에 가두면 그 일을 못 한다. 나가는 것이 맞다.

**약관과 개인정보처리방침의 정본은 웹사이트다**(2026-09-11 대표 지시 — 「웹사이트 기준으로
수정하라. 모든 약관은 여기서 컨트롤한다」). 본문은 `apps/web/src/subpages.ts` 하나이고, 앱·문서·
관리자는 사본을 들지 않는다. 앱의 방침 화면은 이미 그 모양이다 — 요약 셋만 두고 전문으로
내보낸다. 같은 문서를 두 벌 들면 언젠가 한 벌이 낡고, **낡은 쪽을 사용자가 본다.** 실제로
2026-09-09에 그랬다: 앱은 「위탁 업체는 확정 후 명시해요」라고 적고 있었는데 방침에는 수탁자
다섯 곳과 국외 이전 세 건이 이미 올라가 있었다.

**국외 이전만은 고지가 이전보다 먼저다.** 위 「기능 먼저 · 약관은 뒤」의 유일한 예외다 —
만드는 것은 먼저 해도 되지만, **실제로 데이터를 국외로 보내기 시작하는 순간**은 고지 뒤여야 한다. 리전을 옮기고 방침을 나중에 고치면 그 사이가 미고지
이전 구간이 된다 — 국외 이전 자체가 위법이 아니라 고지 없이 이전하는 것이 위법이다
(개인정보보호법 제28조의8). **회사 소재지와 서버 리전을 구분해 적는다** — Neon은 미국 회사지만
데이터는 싱가포르에 있고, 법이 묻는 것은 데이터가 어디로 가느냐다.

**`main`에만 올린다. 하위 브랜치를 쓰지 않는다**(2026-09-15 대표 지시 — 「하위 브랜치는
쓰지 않는다. 무조건 Main에만 올린다」 · 「모든건 너가 최종적으로 main에 올린다」).

**올리는 사람은 MASTER 하나다.** 전담 세션은 지금까지대로 브랜치에서 일하고 PR을 걸되,
`main`에 얹는 것은 MASTER가 한다. 그래서 브랜치는 **넘기는 통로**일 뿐이고 코드가 사는
자리가 아니다 — 며칠 묵은 브랜치가 생기면 그것이 어긋남의 시작이다.

**한 사람만 올리면 빠뜨려도 잡아줄 사람이 없다.** 그 자리를 감시 세션이 맡는다
(`docs/sync/main-integrity-audit.md` · 2026-09-15 대표 지시 「main 업로드 시 누락이 있는지에
대해 너를 감시하는 세션 또 만들어라」).

**CI는 한 번만 돈다 — 통합해서 올린다**(2026-09-15 대표 지시 — 「CI가 너무 많다.
하위 세션들 작업 완료하면 너가 통합해서 하나의 CI로 올려라」).

**브랜치와 PR에서는 CI가 돌지 않는다.** `main.yml`의 `pull_request` 방아쇠를 뺐다.
세션이 브랜치에 밀어도 Actions는 아무것도 하지 않는다.

    세션 브랜치 푸시  →  아무것도 안 돈다
    main 푸시        →  CI + 승인 대기 배포

**CI가 도는 곳은 `main` 하나뿐이다.** 묶는 자리는 브랜치가 아니라 MASTER의 손안이다 —
완료된 브랜치를 **로컬 `main`에 머지**하고, 검사를 **로컬에서 전부 돌린 뒤**, `main`에
**한 번** 민다. 브랜치 열 개가 각자 CI를 돌면 같은 검사가 열 번 돈다. 묶으면 한 번이다.

**`claude/integration` 브랜치는 폐기했다**(2026-09-15 대표 지시 — 「그냥 바로 메인에
올리면 안되는건가?」 · 「바꿔」). 통합 브랜치에서 먼저 돌리고 main에서 또 돌면
**같은 커밋이 두 번 검사된다** — 937(통합) + 938(main) = 30분. 줄이려고 만든 절차가
두 배로 만들었다. 되살리지 않는다.

**바로 `main`에 올려도 빨간 코드가 운영에 나가지 않는다.** `deploy-staging`이
`needs: [ci]`라서 CI가 빨가면 배포 잡은 시작조차 하지 않고 **승인 창도 뜨지 않는다.**
게이트는 `main` 푸시가 아니라 **배포**에 걸려 있다. 통합 브랜치가 막아주던 것은
「main 역사에 빨간 커밋이 한 번 남는 것」뿐이었고, 그 값이 매번 15분이었다.

**대신 올리기 전에 CI가 보는 것을 전부 로컬에서 본다.** 개발 컨테이너에
**PostgreSQL 16이 깔려 있다**(`service postgresql start` · 역할 `weddingpick`) —
api jest까지 여기서 돈다. 「Postgres가 필요해 CI가 본다」는 말은 틀렸다.

    npm run typecheck                                   8개 워크스페이스
    npm run lint
    node lint-copy.js spec/strings.ko.json apps packages
    node scripts/sync-seed-tokens.mjs --check
    DATABASE_URL=postgres://weddingpick:weddingpick@localhost:5432/weddingpick_test npm test

**대신 세션이 로컬에서 다 돌려야 한다.** CI가 안 잡아주므로 잘못 들어온 것은
통합 단계에서 한꺼번에 터진다. 그래서 PR 본문에 **무엇을 돌렸고 몇 건이
통과했는지**를 적게 한다 — 안 돌리고 적은 것은 통합할 때 드러난다.

**통합 커밋의 제목은 «묶은 PR 번호»다**(2026-09-15 대표 지시 — 「CI 제목 정정.
하위 세션들 수정된 PR #번호로 올린다」). Actions 목록에 뜨는 이름이 그 커밋의
첫 줄이라, 거기에 무엇이 들어 있는지가 보여야 한다.

    통합: #245 · #246 · #247 · #248 · #249 · #250 · #251

**번호로 적는 이유**는 짧고, 그대로 눌러 열리기 때문이다. 처음에 브랜치 이름으로
적었더니 일곱 개가 220자를 넘어 Actions 목록에서 잘렸다 — 잘린 제목은 제목이
아니다. 무엇이 들어갔는지는 커밋 본문에 번호마다 한 줄로 적는다.

**올리기 전에 대표님께 브랜치별로 브리핑한다**(같은 지시 — 「최종 CI 올리기 전
나에게 브랜치 별로 뭐뭐가 수정됐는지 간략하게 브리핑한다」). 브랜치 하나에 몇
줄씩, **무엇을 고쳤는지**를 적는다. 파일 목록이 아니라 바뀐 동작을 적는다.
묶은 뒤에는 한 덩어리로 배포되므로, 그 안에 무엇이 있는지 모르는 채로 승인하는
일이 없게 한다.

**워크플로 전체에 `concurrency`를 걸지 않는다.** 2026-09-15에 「중복 실행을 줄이자」고
넣었다가 반나절 만에 두 번 물렸다 — 넣지 말아야 할 자리였다.

    cancel-in-progress: true   →  승인 대기 중인 배포가 접히고 승인 창이 사라진다
    cancel-in-progress: false  →  다음 푸시가 그 뒤에 서서 «승인 전에는 CI도 못 돈다»

둘 다 겪었다. 앞은 07:27에 배포 하나를 날렸고, 뒤는 938이 933의 승인 대기 뒤에
갇혔다. **원인은 하나다 — 실행 안에 «사람을 기다리는 잡»이 들어 있다.** 기계가 언제
끝날지 아는 잡끼리만 줄을 세울 수 있다.

중복 배포는 `deploy-staging`의 `weddingpick-render-production` 그룹과
`자동 배포 커밋 최신 여부` 단계가 이미 막는다. **CI는 겹쳐도 해가 없다.**

**승인을 기다리는 동안 `main`에 또 밀지 않는다.** 밀면 신선도 검사가 앞 실행을 막아,
대표님이 승인해도 배포가 중단된다. 급하면 앞 실행을 접고 새로 민다.

**`main` 푸시 뒤의 현행 흐름은 2026-09-18 KakaoCloud 기준이다.** 이전 호스팅 배포 절차를 다시 살리지 않는다.

    main 푸시  →  CI (typecheck · lint · 카피 린트 · jest 전체)
               →  정적 변경이면 Kakao VM 후보 폴더에 스테이징 (라이브 전환 아님)
               →  운영 API 컨테이너 SHA와 현재 main의 누적 차이 확인
               →  API 변경이 있으면 Runner 복구 → production 승인 → KakaoCloud API + 별도 worker 배포

**DB 마이그레이션은 main 자동배포에 묶여 있지 않다.** `DB Migrate` 수동 workflow가
`environment: production` 아래에서 별도로 실행된다. 이전 호스팅 API는 중지 상태이며 빌드·재배포하지 않는다.
앱웹·관리자·웹사이트는 Kakao 443 공개가 운영 기준이다. 사용자 로그인은 `https://210.109.82.212/login`, 관리자는 `https://210.109.82.212/admin/login`으로 고정하며 별도 공개 포트를 다시 도입하지 않는다.

**main 자동배포는 유지한다.** 다만 CI가 실패하면 API 배포로 가지 않고, 새 main이 들어오면 오래된 CI만
취소한다. 이미 시작된 운영 배포는 별도 concurrency와 VM lock으로 끝까지 보호한다.

**릴리즈 프로덕션 빌드(앱스토어)는 보류다.** 대표님이 「완료」라고 말할 때만 올린다.

**박람회 종료 자동 삭제도 보류다**(2026-09-15 대표 지시 — 「자동 삭제 보류」). 스위치는
`EXPO_AUTO_DELETE_ENABLED`이고 **기본값 꺼짐이 현행**이다. 코드는 운영에 올라가 있고
(`apps/api/src/retention/expo-sweep.ts` · 마이그레이션 `0410`) 켜기만 하면 도는 상태다.

**꺼져 있는 것을 「빠뜨린 것」으로 읽지 마라.** 대표님이 미루라고 하신 것이고, 다시
「켜라」고 하시기 전까지 그대로 둔다. 운영 환경변수에 이 키를 넣지도 않는다.

켜기 전에 할 일은 정해져 있다 — `npm run expo-cleanup --workspace @weddingpick/api -- --dry-run`
으로 **몇 건이 지워질지 먼저 세어 대표님께 보고한다.** 지워지면 본문은 돌아오지 않고
최소 로그(행사명 · 기간 · 장소 · 삭제시각 · 사유)만 남는다.

**서버에 없는 동작은 화면에서 잠그고 목록에도 「조회만」으로 표시한다**(2026-09-10 사용자
지시 — 「서버에 없는 동작들 화면에도 목록 디스에이블 처리해」). 화면 안의
`BACKEND_PENDING`과 `_layout.tsx`의 `READ_ONLY`는 짝이다 — 서버가 붙으면 둘을 함께 지운다.
한쪽만 지우면 말이 어긋난다.

**다만 «아직 못 쓰는 화면»은 메뉴에서 뺀다**(2026-09-15 대표 지시 — 「관리자 메뉴 연결
안되는거 개발 덜된 화면은 노출하지마」). 2026-09-10의 「메뉴를 죽이지는 않는다」가 여기까지
덮고 있었는데, **덮지 말아야 할 자리가 있었다.**

**둘을 가른다. 같은 것이 아니다.**

| 무엇 | 어떻게 |
| --- | --- |
| **읽기는 된다. 쓰기 단추만 없다** | 메뉴에 **보인다** + 「조회만」 딱지. 단추는 잠근다 |
| **읽기도 안 된다. 열면 빈 껍데기다** | 메뉴에서 **뺀다** |

「뒤쪽이 무엇인지는 **재서 정한다**」고 적어 뒀던 것을 **쟀다**(2026-09-16). `READ_ONLY`
다섯이 전부 앞쪽인 줄 알았는데 **셋이 뒤쪽이었다** — 조회가 되는 것이 아니라 서버가 고정값을
돌려주고 있었다.

| 무엇 | 쟀더니 | 했다 |
| --- | --- | --- |
| 가격 통계 · 약관·방침 | **앞쪽이 맞다** — 실제 값을 읽는다 | 그대로 둔다 |
| 업체 문의 | **표가 있는데 안 읽고 있었다** | 붙였다. 넷이 됐다 |
| 이메일 회신 · 수익 현황 | **뒤쪽이다** — 담을 표가 DB에 없다 | 왜 비는지를 라우트에 적었다 |

**업체 문의는 이제 `READ_ONLY`가 아니다.** `structured.vendor_claims`를 읽고 승인·반려가
실제로 돈다 — 대시보드가 진작부터 그 표를 세고 있었다. 넷(이메일 회신 · 가격 통계 ·
수익 현황 · 약관·방침)이 남는다.

**남은 둘을 메뉴에서 뺄지는 아직 정하지 않았다.** 둘 다 사이드바 아홉이 아니라 그 안의
탭이고, 빼는 것은 화면 구성을 바꾸는 일이라 **대표님·마스터가 정한다.**

**숨기는 것은 지우는 것이 아니다.** 라우트는 그대로 살려 둔다 — 주소를 직접 치면 열린다.
사이드바에서만 뺀다. 그래야 서버가 붙었을 때 한 줄로 되돌리고, 그때까지 개발하는 사람은
주소로 열어 볼 수 있다.

**뺀 것은 한 곳에 적어 둔다.** 어느 메뉴를 왜 뺐고 무엇이 붙으면 돌아오는지 — 적어 두지
않으면 **없어진 기능과 숨긴 기능을 구별할 수 없게 된다.** 그때부터 아무도 되돌리지 않는다.

**빈 껍데기를 보여주는 것이 왜 나쁜가.** 운영자는 그 화면을 열고 「고장 났다」고 읽는다.
무엇이 없는지가 아니라 **무엇이 깨졌는지**를 의심하게 되고, 그 의심이 멀쩡한 화면까지 간다.

**관리자 부트스트랩 비밀번호는 OR 판정을 하지 않는다**(2026-09-18 인증 보강).
`ADMIN_PASSWORD`가 설정돼 있으면 그 원문 값만 유효하고, 없을 때만 `ADMIN_PASSWORD_HASH`를 쓴다.
DB 관리자 계정은 scrypt 해시만 저장한다. 두 환경변수를 동시에 둔 채 옛 해시 비밀번호도 통할 것이라고
가정하지 않는다. 운영 권장 방식은 원문 환경변수를 제거하고 해시만 쓰는 것이다.

**마이그레이션 번호는 잡기 전에 확인한다.** 세션을 여럿 돌리면 겹치기 쉽고, 겹치면 하나는
적용 기록만 남고 DDL이 조용히 빠진다 — 2026-09-10에 0102가 겹쳐 시험 셋이 깨졌다. 배정하는
쪽이 번호대를 미리 갈라준다.

**별도 스테이징 환경을 새로 만들자고 먼저 제안하지 않는다.** 현재 `main`이 기준 코드이고,
GitHub-hosted CI가 검증한다. 정적 사이트의 `static-releases/<SHA>` 후보 폴더는 배포 전 검증용이지
별도 사용자 스테이징 서비스가 아니다. 「스테이징에서 확인했다」고 표현하지 않는다.

**운영 배포 기준은 KakaoCloud다.** API는 CI 성공 뒤 live SHA 누적 차이를 확인하고 필요한 경우
production 승인 뒤 같은 main SHA를 배포한다. DB migration은 별도 수동 workflow다. 이전 호스팅은
2026-09-18 폐기된 배포 경로이며 기존 정적 서비스는 전환 중 임시 잔존본일 뿐 새 빌드·배포를 하지 않는다.

**CI 초록과 실제 공개 검증은 구분한다.** 코드 반영 / CI 통과 / API 배포 / 정적 후보 스테이징 /
화면 공개 / 실제 로그인·업로드 검증을 각각 따로 기록한다.

## 진행 상태
- 홈은 C-1 확정. `웨딩픽 홈 5안 비교` 파일은 참고용으로 보존.
- 광고 실운영 전환은 사용자 오더 대기.
- 실제 이미지 대기 중. 규격은 컴포넌트 시트 참조.
