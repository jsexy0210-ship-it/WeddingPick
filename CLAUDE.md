# 웨딩픽 — 구현 규칙

디자인 핸드오프 v3.18(2026-09-08) `PROJECT_RULES.md`가 이 파일의 본문이다. 아래 고정 규칙이
가장 우선이고, 화면 세부(레이아웃 수치 · 상태 목록 · 광고 독립성 · 탈퇴 데이터 · 관리자 로그인 ·
PR 체크리스트)와 2026-09-04~08 정책 변경 이력은 `docs/CLAUDE-rules-archive-v3.17.md`에 그대로 있다 —
이 파일과 충돌하지 않는 한 그쪽도 계속 지킨다. 값(색 · 크기 · 간격 · 문구)은 `spec/tokens.json` ·
`spec/strings.ko.json` · `spec/glossary.json`에서만 가져온다. 하드코딩 금지.

**지침(2026-09-08, 사용자) — 무조건 최신 핸드오프 md 기준으로 바꾼다.** 코드 · 문서 · 이전 규칙과
`docs/design-handoff/current/`의 최신 md(SPEC · CHANGELOG · PROJECT_RULES · screens · tokens)가 충돌하면
묻지 않고 최신 md 쪽으로 코드를 맞춘다. 되묻는 것은 md 자체가 서로 어긋날 때뿐이고, 그때도 먼저
가장 최근 버전 항목(CHANGELOG 상단)을 따른다.

## v3.18 용어 통일 — 확인된 제보 → 실 제보

정보 체계 3단: 상위 개념 **실 제보** · 금액 **제보 금액** · 제공 행동 **Pick 인증**.
금액 표기는 `152~184만원` / `실 제보 12건 · 최근 12개월 · 기준금액 168만원`, 기준금액 설명은
「실 제보의 중앙값이에요」. 코드 치환 규칙 `"확인된 제보" → "실 제보"` — `grep -r "확인된 제보"`가
0이어야 한다(금지어 목록을 적은 줄만 예외). `확인된 정보`도 같은 자리라 같이 바꾼다.

---

## v3.24 구현 원칙(2026-09-08)
- 정보 0층: 실 제보 3건 미만이면 `업체 안내 N만원~`(회색 · 출처)로 대신, 둘 다 없으면 `수집 중` + Pick 인증 CTA.
  금액 한 줄은 어느 화면이든 `priceLine(paidPrice, guidePrice)`(packages/domain guide-price.ts)로만 만든다.
- 스타일은 `WeddingStyle`(URBAN · NATURAL · ROMANTIC · GLAMOROUS) 넷뿐. 화면 라벨은 도시적인 · 자연스러운 · 로맨틱한 · 화려한.
  최소 1 · 최대 2. 태그는 정렬 가중치로만 쓴다 — 태그가 다르다고 업체를 빼지 않는다.
- 홈 상태는 2층(진행 0 · 1~8 · 9+ / 정보량 3건 기준)이다. 준비 현황은 홈 4칸 + WP-HOME-009 두 곳뿐. 코랄은 홈에서 네 곳.
- 검색은 온보딩 값을 미리 걸지 않는다. 알림은 진행 중 업종만 · 하루 2건. 날짜 선택은 WP-APP-023(연월 셀렉트 + 달력).

## 심볼 — 절대 변경 금지
Pick Mark는 하트 안에 체크. 아래 두 path가 확정본이며 어떤 이유로도 바꾸지 않는다.

```
<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">
  <path d="M12 20.5S3.5 15.2 3.5 9.9A4.4 4.4 0 0 1 12 8.1a4.4 4.4 0 0 1 8.5 1.8c0 5.3-8.5 10.6-8.5 10.6Z"></path>
  <path d="M9.4 11.9l1.7 1.7 3.4-3.4"></path>
</svg>
```

- 체크는 하트 중앙 배치. 가로 중심 12.0, 세로 중심 11.9.
- 코드의 원본은 `spec/tokens.json` `symbol` 블록이고 위 path와 같다(2026-09-08 핸드오프판으로 통일).
- 적용처: 앱 아이콘 5종 · 스플래시 · 시작 화면 · Pick 탭 · Pick 완료 모션 · 웹 GNB · 관리자 로그인/사이드바.
- 앱 아이콘과 스플래시는 스킨 컬러와 무관하게 Coral `#FF6F61` 고정, 마크는 White.

## 용어
- 사용자 화면에 `AI` 금지. `웨딩픽이 골랐어요` · `웨딩픽 TOP3` · `웨딩픽 콘텐츠`. 내부 개발·관리자·비용관리 문서에서는 AI 용어 유지.
- 사용자 화면에 `데이터` 금지. `실 제보` · `실 제보 N건` · `많이 확인된 곳` · `아직 정보가 적어요`.
- 정보 체계 3단. 상위 개념 `실 제보` · 금액 `제보 금액` · 제공 행동 `Pick 인증`.
  `확인된 제보` `확인된 정보` `확인된 금액` `실제로 낸 금액` `실제 결제` `실제 결제 데이터`는 쓰지 않는다.
- 애매모호 표현 금지: `거의` `아마` `대략` `어느 정도` `가능성이 높음` `~일 것으로 보임` 근거 없는 `약`·`정도`.
  대신 `확정` `미확정` `검증 전` `확인 필요` `조건부` `측정값` `추정값`.
- `탐색` 금지 → `검색`. `관심업체` 폐기 → `Pick`.
- v3.22 홈 · 혜택 용어: `네이버페이 포인트` → `Npay`(NPay · 네이버 페이 표기도 금지), `우리 준비` → `준비 현황`,
  `오늘의 Pick` → `웨딩픽 추천`. `grep -r`로 잔존 0이어야 한다(금지어 목록을 적은 줄만 예외).
- 혜택 화면에 운영 기간을 적지 않는다: `하루 안에` `매달` `~까지` `30일 안에` `마감일` `당첨 확률` 금지.
  「확인 후 알려드려요」 「새 회차가 열리면 알려드려요」로 쓴다. 완료된 기록의 날짜(8월 21일 완료)는 남긴다.
  `~하지 않아요` `~못해요` `~안 해도`로 끝나는 안내 금지 — 무엇이 되는지를 말한다.
- 혜택 안내 시트(WP-SHT-017): 온보딩 완료 후 홈 최초 진입 1회 · 400ms 뒤. 닫으면 `sheetSeen` 저장. 남은 응모 조건
  2개 「두 가지만 더 하면」 · 1개 「하나만 더 하면」 · 0개면 시트 대신 응모 완료 알림. Npay 로고는 CSS로 그린다.

## 디자인
- SEED Design System(Karrot) 기반. 좌우 Gutter 24px.
- 스킨 6종: Coral #FF6F61(기본) · Red #FF4D4D · Yellow #FFC041 · Green #34C759 · Blue #3182F6 · Dark Gray #191F28.
- 폰트는 시스템 서체 유지(Pretendard 미적용).
- 섹션 제목 1줄, 서브카피 사용하지 않음. 안내 최대 2줄, CTA 1줄.
- 화면당 Primary CTA 1개. Pick이 가장 중요한 행동, 비교는 보조.

## 예시 데이터
- 웹은 가명 업체(`강남 A 스튜디오` 형식)와 샘플 이미지를 유지한다. 실제 상호와 실제 촬영물을 노출하지 않는다.
- 앱은 실제 업체명과 실제 이미지를 쓴다. 시안의 가명은 개발 시 실제 데이터로 대체한다.
- 금액은 웹·앱 모두 실 제보로 집계한 실제 구간을 쓴다.

## 진행 상태
- 홈은 C-1 확정. `웨딩픽 홈 5안 비교` 파일은 참고용으로 보존.
- 광고 실운영 전환은 사용자 오더 대기.
- 실제 이미지 대기 중. 규격은 컴포넌트 시트 참조.
