# 미리보기 런타임

`react18-runtime.js`는 설치 없이 미리보기를 열기 위한 React 18.2.0, React DOM 18.2.0, Scheduler 0.23.0의 프로덕션 런타임입니다. MIT 라이선스 전문은 `LICENSE-React.txt`에 있습니다.

React 소스 개발 및 Vite 빌드에서는 `package.json`의 React 의존성을 사용합니다. 이 파일은 `npm run build:preview`에서 단일 HTML을 만들 때만 사용합니다. 폰트 파일은 포함하지 않았습니다.
