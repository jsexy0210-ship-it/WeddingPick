# 공통 작업 원칙

모든 규칙의 공유 기준은 최신 `main`이다. 규칙을 변경하면 같은 작업에서 PR을 통해 main에 반영하고 병합 결과를 확인한다. 로컬 파일·개별 브랜치·세션 대화에만 남겨두지 않는다.

## 마스터·전담 세션 운영 — 2026-09-10 사용자 확정

- 이 프로젝트의 요구사항은 마스터 세션이 접수하고 필요한 전담 세션을 생성·재사용해 배정한다. 소유 파일과 완료 기준을 먼저 정해 중복 작업을 막는다.
- 마스터는 구현자의 완료 보고를 그대로 승인하지 않는 독립 검수자다. 오류·회귀·누락·정책 불일치·허위 성공·권한·캐시 오염·사용자 여정을 반례로 검토하고, 실패한 항목을 수정 후 재검증한다.
- 전담 세션은 관련 검사와 근거를 갖춘 PR을 제출한다. 마스터는 최신 main 및 클로드 작업 PR과의 충돌·의존성·통합 검증을 확인한 뒤 병합한다.
- 완료되거나 불필요한 세션은 결과·PR·남은 문제를 마스터 상태에 흡수한 뒤 보관한다. 미완료 작업을 완료로 취급하거나 미커밋 파일·Git 이력을 삭제하지 않는다.

- 정확성, 최소 변경, 최소 컨텍스트 사용을 우선한다.
- 요청 범위의 관련 파일만 검색하고 확인한다. 전체 탐색·동일 파일 반복 읽기·과잉 탐색을 피한다.
- 검색 → 관련 파일 확인 → 최소 수정 → 필요한 검증 → 종료 순으로 진행한다.
- 기존 코드·구조를 재사용하고 불필요한 추상화·중복 코드·임시 우회를 만들지 않는다.
- 단순 작업은 바로 처리한다. 서브에이전트는 대규모 탐색 또는 독립 작업 병렬화가 실제로 필요할 때만 사용한다.
- 변경 범위에 맞는 최소 테스트부터 실행하며 매번 전체 검증하지 않는다.
- 결과와 중요한 문제만 간결하게 보고한다. 긴 로그·코드·탐색 과정·기존 정보를 반복 출력하지 않는다.
- 기존 미커밋 변경을 보호하며 임의로 reset/revert/delete 하지 않는다.
- 프로젝트 CLAUDE.md 및 최신 Git 상태를 우선 확인하고, 세션 대화보다 최신 main의 코드·정책·상태 문서를 Source of Truth로 사용한다.
- 완료 정보를 불필요하게 누적하지 않고, 작업 주제가 바뀌면 관련 없는 컨텍스트를 끌고 가지 않는다.
- 불필요한 파일·문서·캐시·로그를 생성하거나 누적하지 않는다.
- 비용이 발생할 수 있는 외부 서비스·API·인프라 변경은 필요성을 먼저 확인한다.

아래 프로젝트 규칙은 관련 작업에 적용한다. 작업 방식이 충돌하면 위의 최신 공통 원칙을 우선한다. main의 상태 문서도 확인 날짜·소스 커밋·검증 범위를 함께 읽는다.

# 작업 운영 규칙

## 최상위 구현·검수 정책 — 사용자 지시 2026-09-05

### 최신 일괄 변경 — RN 셸 + 하이브리드 앱

- WP-RPT-002 이미지 선택·촬영은 RN으로 구현한다.
- WP-NOTI-003은 OS 권한·푸시 연결을 RN에서 담당하고 설정 UI는 하이브리드로 구현한다.
- WP-NOTI-003 야간 수신 끔: 한국시간 21:00 이상~다음 날 08:00 미만 푸시를 생략한다. 알림함은 유지하며 아침에 누적 푸시를 재발송하지 않는다(사용자 승인 2026-09-06).
- 이외 앱 화면은 하이브리드로 전환한다. 추가 네이티브 기능이 필요하면 해당 화면 ID와 사유를 기록하고 네이티브 구현을 추가한다.
- 비로그인 앱 이용을 폐기한다. 스플래시 이후 미인증 사용자는 로그인 화면으로 진입하며 딥링크도 동일하게 보호한다. 기존 파일·기록을 삭제하는 의미는 아니다.
- WP-HOME-001 메인 헤더 검색 버튼을 제거하고 알림만 유지한다.
- 기존 디자인 완료 웹사이트 `apps/web`는 전환 대상에서 제외하며 현 상태를 유지한다. 앱용 하이브리드 화면과 별도 제품으로 취급한다.
- 디자인 확인용 개발 미리보기는 운영 인증과 분리하고 명시적으로 표시한다. 운영 빌드에 인증 우회를 제공하지 않는다.


- 사용자 확정: WP-HOME-001 레이아웃 충돌은 전달 HTML·PNG 시각 디자인을 최종 기준으로 한다. 준비현황 2×2, 추천 3열. 해당 홈 레이아웃에서는 충돌하는 screens.json 표현보다 이 결정이 우선한다.

- 모든 화면은 `docs/design-handoff/current/screens.json`의 화면 ID 단위로 전달 디자인과 1:1 매칭한다. 기존 코드나 개발자의 선호를 근거로 디자인을 바꾸지 않는다.
- 각 화면 ID에 한글명, 기준 HTML/PNG와 해당 화면 위치, 실제 라우트·컴포넌트, 회원/비회원 및 상태 분기, API 계약, 검수 증거를 연결한다. 화면 ID 언급만으로 매칭 완료로 처리하지 않는다.
- 레이아웃·세로 순서·문구·타이포·색상·간격·크기·아이콘·이미지 규격·safe area·상호작용을 해당 ID 명세 그대로 구현한다. 공통 UI는 SEED 기준을 사용하며 임의 이모지·유사 아이콘·스타일로 대체하지 않는다. 전달된 웨딩픽 브랜드 토큰·전용 심볼을 따른다.
- 원본과 실제 구현의 동일 상태를 390×844 기준으로 비교한다. 기준 캡처와 실제 캡처, 차이 및 조치 결과가 없으면 시각 검수 미완료다. 웹 캡처는 네이티브 검증으로 간주하지 않는다.
- 명세 충돌·누락·원본 부재는 해당 화면 ID에 기록하고 사용자에게 확인한다. 임의로 선택하거나 구현된 기존 화면을 정답으로 삼지 않는다.
- 모든 관련 검수 통과 전 해당 화면을 완료로 보고하지 않는다. 원본 PNG를 실제 앱 캡처로 제시하지 않는다.
- 이 정책은 프로젝트 내 기존 구현 관례·작업 편의보다 우선한다. 이후 사용자의 명시적인 변경 지시 없이 완화하지 않는다.

- 한국어로 결론부터 간결하게 보고한다.
- 마스터 세션이 전체 작업, 통합, 독립 검토를 책임진다.
- 독립적으로 진행할 수 있는 작업은 하위 에이전트에 병렬 배정한다. 파일 소유 범위를 정하고 공유 파일 충돌을 피한다.
- 최신 사용자 디자인 전달본은 `docs/design-handoff/current/`다. 디자인 수치는 `tokens.json`, 화면 구조는 `screens.json`, 기능은 `SPEC.md`를 함께 확인한다. 이전 `spec/`와 충돌하면 차이를 기록하고 최신 사용자 지시를 적용한다.
- 기존 자료·브랜치·Git 이력을 일괄 삭제하지 않는다.
- 커밋과 푸시 전에 변경 전체를 검토하고 문제를 수정한다. 관련 테스트·타입 검사·UI 검수가 미완료이거나 문제·차단 사항이 있으면 커밋·푸시하지 않는다.
- 체크리스트는 `docs/sync/checklist.json`, 작업 상태는 `docs/sync/*-status.json`, 화면 점검은 `docs/sync/screen-audit.json`에 기록한다.
- 화면 파일의 존재, 화면 ID 주석, 타입 검사 통과를 디자인·기능 완료로 간주하지 않는다. 실제 UI/API/DB/관리자 연결과 상태별 동작의 증거가 필요하다.
- 운영 DB에 테스트 데이터 입력·초기화·리셋을 하지 않는다. 테스트는 분리된 폐기 가능한 DB에서 실행한다.
- 사용자 체크리스트: 반응형/여백/긴 텍스트·빈 상태, 토큰/위계/테마, 기본·포커스·눌림·비활성 상태, 이미지·아이콘·모션, 컴포넌트 재사용·레이어를 검수한다.
