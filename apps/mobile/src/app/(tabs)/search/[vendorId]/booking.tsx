/**
 * `/search/:vendorId/booking` — 피그마 `VendorFlows.tsx` `export { ConsultPage as BookingPage }`.
 * 규격서 vendor-1-booking.txt는 vendor-1-consult.txt와 같은 79 노드다 — 화면 하나를 두 주소에서 연다.
 */
export { default } from './consult';
