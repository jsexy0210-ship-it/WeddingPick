import { LoungeScreen } from '@/features/community/lounge-screen';

/** 정본 my.jsx frame-012 — 박람회 · WP-LNG-003. */
export default function LoungeExpoScreen() {
  return <LoungeScreen kind="expo" />;
}
