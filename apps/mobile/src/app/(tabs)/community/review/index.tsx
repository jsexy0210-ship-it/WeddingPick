import { LoungeScreen } from '@/features/community/lounge-screen';

/** 정본 my.jsx frame-008 — 리얼후기 · WP-LNG-001. */
export default function LoungeReviewScreen() {
  return <LoungeScreen kind="review" />;
}
