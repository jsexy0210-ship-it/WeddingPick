import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function HomeScreen() {
  return (
    <ScreenPlaceholder
      screenId="A-03"
      title="홈"
      phase={1}
      summary="촬영 CTA 중심. 최근 분석 결과와 진행 중인 웨딩 요약을 보여준다."
    />
  );
}
