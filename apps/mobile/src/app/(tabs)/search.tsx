import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function SearchScreen() {
  return (
    <ScreenPlaceholder
      screenId="A-16"
      title="검색"
      phase={2}
      summary="업체·플래너 탐색. Phase 2(Compare) 대상이라 Phase 1 범위에는 들어가지 않는다."
    />
  );
}
