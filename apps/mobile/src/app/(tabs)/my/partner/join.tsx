/**
 * 초대 수락 별칭 — 연결관리와 같은 스택 안(`features/partner/routes.ts` `partnerJoinRoute`).
 * 화면은 `wedding/join.tsx` 하나다. 주소가 `…/partner/join`이라 Back은 계층대로 연결관리다.
 */
export { default } from '../../wedding/join';
