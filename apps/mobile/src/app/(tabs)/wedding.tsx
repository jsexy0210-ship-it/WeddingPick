import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function WeddingScreen() {
  return (
    <ScreenPlaceholder
      screenId="A-11"
      title="내 웨딩"
      phase={1}
      summary="후보·견적·가계약·계약·비용 목록. 배우자 연결(Phase 3) 후에는 '우리 웨딩'이 된다."
    />
  );
}
